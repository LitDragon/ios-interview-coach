# iOS 深水主题卡：线上 Crash 怎么闭环

## 主题

线上 Crash 怎么闭环

## 面试官常问法

> 线上 Crash 你怎么收集、分析、修复、验证？给我讲一个你处理过的 Crash 案例。

## 核心回答

我不会只看崩溃日志就修代码。线上 Crash 闭环是：**采集 → 聚合 → 归因 → 修复 → 验证 → 监控**，每个环节都不能缺。

采集要保证符号化和用户场景完整性；归因要区分是代码 Bug、系统兼容、还是三方库问题；修复后要验证符号表对得上、场景能复现；最后要看修复版本上线后这类 Crash 是否真正下降。

## 排查顺序

### 1. 采集

- 系统自带：`CrashReporter`、`.ips` 文件、Xcode Organizer。
- 三方平台：Bugly、Firebase Crashlytics、Sentry。
- 自建：Mach 异常处理、`NSSetUncaughtExceptionHandler`、信号捕获。
- 关键信息：堆栈、设备型号、系统版本、App 版本、内存、电量、用户操作路径。

### 2. 聚合

- 按堆栈签名聚合，不是按单条日志。
- 区分同一类 Crash 的不同变体（不同系统版本、不同设备）。
- 关注影响用户数和 Crash 率，优先处理 Top 问题。

### 3. 归因

- **野指针 / EXC_BAD_ACCESS**：访问已释放对象，查生命周期、线程安全。
- **越界 / SIGABRT**：数组越界、强制解包、未实现的方法。
- **死锁 / 主线程阻塞**：主线程同步等待、锁嵌套。
- **系统兼容**：私有 API 变更、系统版本差异、权限变化。
- **内存压力**：OOM、内存泄漏、大图加载。
- **第三方库**：版本 bug、API 变更、与系统不兼容。

### 4. 修复

- 能复现的：写单元测试或 UI 测试覆盖。
- 不能复现的：加日志、加防护、灰度验证。
- 野指针：开启 Zombies、用 Address Sanitizer。
- 越界：改用安全访问方法、加 guard 判断。
- 主线程卡死：异步化、加超时、避免嵌套锁。

### 5. 验证

- 符号化验证：dSYM 匹配、地址还原。
- 场景验证：相同路径、相同设备、相同系统版本。
- 灰度验证：小范围放量观察 Crash 率。

### 6. 监控

- 版本维度：新版本 Crash 率是否下降。
- 问题维度：修复的那类 Crash 是否消失。
- 回归维度：修复是否引入新问题。

## 3 分钟面试回答版本

我处理线上 Crash 的闭环是六个环节：采集、聚合、归因、修复、验证、监控。

采集阶段，我们用 Bugly 加自建信号捕获，保证堆栈、设备信息、内存状态都能拿到。聚合阶段按堆栈签名分组，不是看单条日志，而是看同一类问题影响多少用户。

归因阶段我会先分类：野指针、越界、死锁、系统兼容、内存压力，每类排查思路不同。比如野指针要开 Zombies 和 Address Sanitizer 复现；越界要查数据源和强制解包；死锁要查主线程阻塞和锁嵌套。

修复后不能直接上线，要先验证符号化堆栈对得上，再用灰度小范围放量看 Crash 率是否下降。最后在新版本全量后持续监控，确认问题真正闭环。

## 项目案例模板

我之前处理过一个线上 Crash，现象是部分用户在首页滑动时崩溃，堆栈指向 `UITableView` 的 `reloadData`。

归因后发现是数据源在后台线程被修改，主线程 `reloadData` 时数组已变。当时没有加锁，因为之前数据源只在主线程操作，后来加了一个后台预加载功能，引入了竞态。

修复方案是把数据源操作统一收口到主线程，后台预加载只负责准备数据，不直接修改数据源数组。改完后用 Address Sanitizer 跑了一遍确认没有其他内存问题，灰度放量后这类 Crash 从 Top 3 降到 0。

## 面试追问准备

### 1. 野指针 Crash 堆栈不准怎么办？

野指针 Crash 的堆栈往往是崩溃点，不是原因点。要开 Zombies 看具体是哪个对象被释放后访问；用 Address Sanitizer 做内存检查；如果线上无法复现，就加防护日志，记录可疑对象的生命周期。

### 2. 符号化失败怎么办？

检查 dSYM 是否匹配（UUID 对比）、是否上传到平台、是否是系统库堆栈（需要对应系统符号表）。如果 dSYM 丢失，只能靠堆栈地址和偏移量估算。

### 3. 怎么区分 OOM 和 Crash？

Crash 有系统堆栈，OOM 没有。判断 OOM 看 `jetsamEvent` 日志、内存峰值、后台被杀情况。可以用 `memory pressure` 监控和 `didReceiveMemoryWarning` 提前预警。

### 4. 线上 Crash 修复优先级怎么定？

看三个维度：影响用户数、Crash 率、是否阻塞核心流程。影响用户多且阻塞核心流程的优先级最高。

### 5. 怎么处理三方库 Crash？

先查 issue 看是否已知问题，有新版就升级；如果无法升级，就绕过或降级；如果是系统兼容问题，要加版本判断做适配。

## 简历 bullet

建立线上 Crash 采集、聚合、归因、修复、验证、监控闭环流程，处理野指针、越界、死锁、系统兼容等多类 Crash，通过灰度验证和版本监控确保修复有效性，持续将 Crash 率控制在 0.1% 以下。

## 核心记忆句

闭环六步：采集 → 聚合 → 归因 → 修复 → 验证 → 监控，缺一环都不算真正修好。

## 风险点

- 不要只说"我看了 Crash 日志然后修了"，面试官会追问你怎么归因、怎么验证。
- 不要忽略 dSYM 管理，符号化失败会让整个采集链路失效。
- 不要承诺"Crash 率降到 0"，线上环境复杂，0.1% 以下是更合理的说法。
- 不要只用 Bugly 截图回答，要能解释堆栈怎么还原、怎么聚合。
- 如果没有线上 Crash 处理经验，就用"本地复现 + Instruments 排查"表达思路，别编线上数据。
