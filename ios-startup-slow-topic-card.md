# iOS 深水主题卡：App 启动慢怎么排查

## 主题

App 启动慢怎么排查

## 面试官常问法

> 你们 App 启动慢，你会怎么定位和优化？

## 核心回答

我不会先直接改代码，而是先定义“慢”发生在哪一段。iOS 启动可以先拆成两部分：`main` 之前和 `main` 之后。

`main` 之前主要看动态库加载、符号绑定、Objective-C `+load`、静态初始化等；`main` 之后主要看 `didFinishLaunching`、首页初始化、同步 I/O、网络请求、数据库、埋点 SDK、推送 SDK、广告/配置拉取这些是否阻塞首屏。

我会先用 Instruments、Time Profiler、`os_signpost` 做分段测量，确认瓶颈，再决定是延迟初始化、异步化、按需加载、减少启动依赖，还是拆掉不必要的同步任务。

## 排查顺序

### 1. 先定义指标

- 冷启动还是热启动。
- 从点击图标到首屏展示，还是到首屏可交互。
- 是所有用户慢，还是特定设备、系统版本、App 版本慢。

### 2. 先分段

- `main` 前慢：查动态库、`+load`、静态初始化、三方库数量。
- `main` 后慢：查 `AppDelegate`、`SceneDelegate`、首页构建、同步任务。

### 3. 再找阻塞点

- 启动时同步读大文件。
- 启动时初始化太多 SDK。
- 首页一次性创建过多 View / ViewModel。
- 数据库迁移或缓存清理放在主线程。
- 启动时等待网络配置返回。
- 图片解码、JSON 解析、日志上传阻塞主线程。

### 4. 最后选优化策略

- 非首屏需要的 SDK 延迟初始化。
- 网络请求不阻塞首屏。
- 数据库迁移放到合适时机，并加版本判断。
- 首页数据分批加载，先展示骨架或缓存。
- 重任务移出主线程，但 UI 生命周期相关逻辑保持主线程正确。
- 删除无用动态库和无意义的 `+load` 初始化。

## 3 分钟面试回答版本

我处理启动慢会先避免凭感觉优化。第一步是定义启动指标，比如冷启动从点击图标到首页可交互。第二步把启动拆成 `main` 前和 `main` 后。

如果是 `main` 前慢，我会重点看动态库数量、Objective-C `+load`、静态初始化和三方库引入成本。如果是 `main` 后慢，我会看 `didFinishLaunching`、首页初始化、数据库、缓存、埋点、推送、网络配置这些有没有同步阻塞。

实际优化上，我通常会把非首屏必要的 SDK 延迟初始化，把启动阶段的网络等待改成异步，把大文件读取、缓存清理、日志上传移出启动关键路径。首页也不会一次性加载所有模块，而是先保证首屏可见和可交互，再补充非关键数据。

最后我会用同一台设备、同一套场景复测，避免只靠主观感觉判断优化是否有效。

## 项目案例模板

我之前遇到过一次启动慢，现象是冷启动进入首页明显卡住。排查后发现问题不在 UI 渲染本身，而是启动阶段同时做了多个非首屏任务，包括 SDK 初始化、缓存读取和接口配置拉取。

我把启动链路拆成首屏必需和非必需两类：首屏必需的保留在主路径，非必需的延迟到首页展示后或用户进入相关功能时再执行。改完后首屏出现更快，也降低了启动阶段主线程阻塞风险。

## 面试追问准备

### 1. 如果启动慢只在低端机出现怎么办？

按设备分层看数据，低端机重点查主线程重任务、图片解码、首页 View 层级、数据库和 I/O。

### 2. 所有 SDK 都说自己要启动时初始化怎么办？

区分强依赖和弱依赖。推送、崩溃采集可能要早；广告、统计、配置、客服、分享等通常可以延迟或按需。

### 3. 异步化会不会引入问题？

会，所以不能简单丢到后台线程。要保证 UI 状态、依赖顺序、线程安全和失败兜底，尤其是登录态、配置、路由和首页数据。

## 简历 bullet

优化 App 冷启动链路，拆分启动阶段首屏必需与非必需任务，延迟初始化非关键 SDK 和后台任务，降低启动阶段主线程阻塞风险，提升首页可见与可交互速度。

## 核心记忆句

先分段测量，再把非首屏必需的事情移出启动关键路径。

## 风险点

- 不要一上来就说“我会异步化”，面试官会追问线程安全和依赖顺序。
- 不要只讲工具，要讲你怎么判断哪些任务可以延迟。
- 不要承诺具体优化百分比，除非你真实测过。
- 如果没有真实启动优化经历，就用“排查思路 + 小规模优化经验”表达，别编线上数据。
