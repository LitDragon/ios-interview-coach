# OC与通用工程高级面试题排序版

> 目标：按高级 iOS 面试中的重要性和区分度排序。答案采用面对面口语表达，每题建议控制在 60 到 90 秒。
>
> 筛选原则：删除单纯 API 记忆、代码默写、低阶概念题；把必要基础合并到更高级的问题里回答。

---

## 回答结构模板

每题套用以下框架，即使记不全细节也能组织出完整回答：

1. **一句话结论**："这个机制本质是..."
2. **核心原理**：2-3 句，讲清流程
3. **我怎么做**：1-2 句，讲实际处理
4. **风险/取舍**：1 句，体现工程判断

---

## 题目分级

| 优先级 | 定位 | 题量 |
| --- | --- | --- |
| P0 | 必须讲清楚，决定高级工程师基本面 | 10 题 |
| P1 | 高频进阶题，体现项目深度 | 9 题 |
| P2 | 加分题，体现边界经验和工程判断 | 6 题 |
| 架构篇 | 架构设计与工程治理，按需考察 | 2 题 |

---

# P0：高级 iOS 必会题

## 01：你怎么理解 iOS 的内存管理？ARC 到底解决了什么，没有解决什么？

**口语答案：**

> ARC 本质上还是引用计数，不是垃圾回收。它解决的是 retain、release 这些所有权操作由人工维护容易出错的问题，编译器会根据对象的生命周期，在合适的位置插入持有和释放逻辑。
>
> 但 ARC 没有解决所有内存问题。比如循环引用、缓存无限增长、大图解码占用、C 指针或 Core Foundation 对象管理，这些仍然需要工程师自己判断。weak 的自动置空也依赖运行时维护的弱引用表，并不是简单地"不持有"这么一句话。
>
> 面试里我会强调：ARC 让普通对象生命周期更安全，但高级开发仍然要能判断谁拥有谁、对象什么时候释放、异步回调里会不会延长生命周期，以及是否有非 Objective-C 对象需要手动桥接和释放。

---

## 02：项目里出现内存泄漏，你通常怎么定位和修复？

**口语答案：**

> 我会先确认是不是持续增长，不能只看某一瞬间的内存峰值。比如图片列表快速滑动时内存上升是正常的，但退出页面后对象还留着，就要重点看引用链。
>
> 在研发阶段，我们主要依赖自动化检测拦截：在 Debug 阶段或 CI 流程中集成 MLeaksFinder 和 FBRetainCycleDetector，自动发现并弹窗提示内存泄漏与强引用环。对于复杂的长链路，再配合 Xcode Memory Graph 或 Instruments 的 Leaks 进行堆栈回溯。
>
> 修复时不会只机械地加 weak，而是要理清所有权关系，彻底打断强引用环，并对短生命周期任务添加防护，避免对象提前释放。

---

## 03：Block 捕获变量和循环引用，你会怎么给面试官讲？

**口语答案：**

> 我会先说结论：Block 是对象，逃逸后通常会被拷贝到堆上；它捕获外部变量时，会根据变量类型产生不同的持有或拷贝行为。普通局部变量更像是捕获当时的值，对象变量会涉及强引用，__block 变量会被包装成一个可共享的结构。
>
> 循环引用的核心不是"Block 一定会循环"，而是"谁持有 Block，Block 又是否强持有谁"。比如控制器持有一个网络回调 Block，Block 里直接使用 self，就可能形成控制器到 Block、Block 到控制器的闭环。
>
> 我的处理习惯是：需要避免闭环时用 weak 打断；进入回调后如果需要保证执行期间 self 不释放，再临时转强引用。更重要的是判断业务语义，有些短生命周期的同步 Block 不需要过度 weak，否则反而可能导致关键逻辑执行一半对象消失。

---

## 04：你怎么设计线程安全？GCD、锁、OperationQueue 怎么取舍？

**口语答案：**

> 我会先区分两类问题：一类是把耗时任务移出主线程，另一类是共享状态的线程安全。前者用 GCD 或 OperationQueue 都能解决；后者要明确保护的是哪段临界区，不能只说"异步一下"。
>
> GCD 适合简单任务调度，比如异步 IO、回主线程刷新 UI、用串行队列保护资源。OperationQueue 更适合有依赖、取消、最大并发数、任务状态管理的场景。锁适合保护非常小的同步临界区，比如 os_unfair_lock、递归锁、读写锁，各自都有适用边界。
>
> 我会特别避免几个坑：不要在主队列同步派发到主队列；不要用 atomic 误以为对象整体线程安全；不要把大任务放在锁里；也不要让多个队列交叉等待。高级一点的做法是尽量减少共享可变状态，让数据流单向，必要时再用队列或锁做明确保护。

---

## 05：什么是卡顿？你怎么监控、定位和优化？

**口语答案：**

> 卡顿本质是主线程没有在一帧预算内完成布局、提交和渲染。60Hz 大约 16.7 毫秒一帧，120Hz 更严格。
>
> 主线程 RunLoop 负责处理触摸、Timer、布局和渲染事件。如果主线程长时间停在同步 IO、复杂布局、锁等待上，就不会在一帧内完成提交，用户就看到掉帧。
>
> 监控上，线下用 Instruments 的 Time Profiler 和 Core Animation；线上通过 RunLoop 观测或子线程 watchdog 抓主线程堆栈，注意控制采样成本。优化先看证据，常见方向是主线程同步解析、Auto Layout 过多、图片主线程解码、锁竞争。优化后要用同一场景复测帧率确认效果。

---

## 06：启动优化你会从哪些阶段下手？

**口语答案：**

> 我会把启动分成 main 函数前和 main 函数后。main 前主要是系统加载可执行文件、动态库、类注册和 load 方法，优化重点是减少动态库数量、控制 +load、减少不必要的全局初始化。
>
> main 后是业务启动链路，包括 AppDelegate、首屏路由、配置读取、网络预热等。这里要区分首屏必要任务和可延后任务，能懒加载的不要阻塞首屏，能异步的不要占主线程。
>
> 我通常会先建立启动耗时分段指标，再看 Time Profiler 找瓶颈。更稳的做法是按依赖关系拆启动任务，保证首屏可用，同时让非关键 SDK 在合适时机初始化。

---

## 07：复杂列表或首页滑动不流畅，你会怎么优化？

**口语答案：**

> 列表优化从数据、布局、渲染、图片四个方向看。数据层避免滚动中同步解析；布局层缓存高度或提前生成展示模型；渲染层减少层级、透明混合和阴影裁剪；图片层控制尺寸、异步解码和缓存策略。
>
> Cell 复用只是基础，高级场景更容易出问题的是复用状态污染、异步图片回调错位、预取和取消不完整。我会先打开帧率和主线程耗时工具定位瓶颈，优化后验证快速滑动、低端设备和弱网场景。

---

## 08：Runtime 的消息发送机制是什么？为什么它对高级 iOS 很重要？

**口语答案：**

> Objective-C 的方法调用本质是消息发送。对象收到消息后，会先根据 isa 找到类，再从方法缓存和方法列表里找 selector 对应的实现，找不到就沿继承链继续找。
>
> 如果最终还是找不到，并不是马上崩溃，Runtime 会给几次补救机会：动态方法解析、快速转发、完整消息转发。全部处理不了，才会出现 unrecognized selector。
>
> 这件事重要，是因为很多高级能力都建立在 Runtime 上，比如 KVO 的动态子类、Method Swizzling、Category 方法加载、埋点、消息转发型路由。面试里我会重点讲清楚"动态性带来能力，也带来风险"。

---

## 09：Objective-C 对象的内存结构是什么？isa 指针起什么作用？

**口语答案：**

> OC 对象在内存里第一个字段就是 isa 指针，指向它所属的类。类本身也是对象，它的结构里包含 superclass 指针、方法缓存 cache、方法列表 bits 这些信息。
>
> 关键点是类方法存在哪。每个类有一个对应的元类，类方法实际存在元类的方法列表里。元类的 isa 最终指向根元类，根元类的 isa 指向自己，形成闭环。消息发送时，对象方法沿类链找，类方法沿元类链找。
>
> 这个知识在 Runtime 题里是基础。理解了 isa 和元类，才能讲清楚 KVO 的动态子类、消息转发的查找路径、Category 方法合并这些高级话题。面试里不需要画结构体，但要能说清"对象的 isa 指向类，类有缓存和方法列表，类方法存在元类里"这个链路。

---

## 10：线上 Crash 你怎么排查？怎么把稳定性做成体系？

**口语答案：**

> 我会先看 Crash 是否符号化完整，再按影响面、版本、设备、系统、页面路径聚类。单个堆栈不能直接下结论，要结合用户行为、日志、埋点、最近发布 diff 和异常上下文。
>
> 常见 Crash 类型包括数组越界、字典插入 nil、野指针、KVO 或通知释放问题、主线程 UI 违规、并发读写、unrecognized selector、OOM。不同类型排查方式不一样，比如 OOM 通常没有标准崩溃堆栈，需要看内存峰值和页面路径。
>
> 体系化上，我会做崩溃率、卡死率、OOM、启动失败、关键路径错误率这些指标，并且给高风险模块加防御和灰度。高级工程师不能只会修一个 Crash，还要能降低同类问题再次出现的概率。

---

# P1：高频进阶题

## 11：KVO 的底层原理是什么？实际项目里有哪些风险？

**口语答案：**

> KVO 的核心是运行时动态子类。对象被观察后，系统会生成一个 NSKVONotifying_ 前缀的中间子类，把对象的 isa 指过去，并重写被观察属性的 setter，在赋值前后插入通知逻辑。
>
> 所以 KVO 对调用路径有要求：通过 setter 修改才能自动触发，直接改成员变量不会触发；可变集合内部变化也不一定符合预期。它还有重复添加、移除时机、上下文区分、线程回调位置这些风险。
>
> 我的使用原则是：简单状态观察可以用，但要封装观察生命周期，使用明确的 context。如果已有明确的回调链路或 RAC 信号，我会优先选择更可控的方案。

---

## 12：离屏渲染是什么？圆角、阴影、mask 应该怎么优化？

**口语答案：**

> 离屏渲染分为 GPU 离屏（GPU 切换上下文）和 CPU 离屏（Core Graphics 自定义绘制）。优化核心在于减少不必要的渲染开销。
>
> 对于圆角，优先通过设计侧切好带圆角的图片，或在后台用 UIGraphicsImageRenderer 生成圆角图片；阴影必须指定 shadowPath，明确形状后 GPU 不需要实时遮罩计算。shouldRasterize 仅适用于复杂但静态的视图，列表滚动等频繁刷新的场景不适合。
>
> 工具验证用 Instruments 的 Core Animation 开启 "Color Offscreen-Rendered Yellow"，在真机上对列表滑动进行全场景验证。

---

## 13：大图、图片缓存和内存峰值你怎么处理？

**口语答案：**

> 图片问题不能只看文件大小，更要看解码后的内存占用。一张压缩后几百 KB 的图片，按像素展开后可能占几十 MB，如果在主线程解码，还会同时造成内存峰值和卡顿。
>
> 我的处理方式是按展示尺寸请求或裁剪图片，避免加载远大于显示尺寸的原图；下载后异步解码；内存缓存要有数量和成本限制；收到内存警告或进入后台时及时释放可重建缓存。
>
> 在列表里还要处理复用和取消。Cell 复用后旧请求必须取消或校验标识，否则会出现图片错位。预加载也要有边界，弱网和快速滑动时盲目预取会让网络、CPU、内存一起恶化。

---

## 14：网络层你会怎么设计，才能支撑大型业务？

**口语答案：**

> 网络层架构上我采用业务层、适配层、传输层三层解耦，业务层只感知强类型 Model，底层统一处理公共 Header 注入、Token 刷新和错误码映射。
>
> 高可用方面，可以引入 HTTPDNS 绕过运营商 DNS 劫持，弱网场景推动 HTTP3 落地利用无队头阻塞特性。另外必须建立网络 APM 打点，对 DNS、TCP 握手、SSL 握手、响应传输各阶段耗时做监控，才能精准定位瓶颈。

---

## 15：本地缓存和数据一致性你怎么做？

**口语答案：**

> 我会按数据价值和一致性要求分层：临时状态数据用 MMKV 代替 UserDefaults，底层通过 mmap 实现零拷贝，读写速度快几个数量级；复杂关系型数据用 SQLite 或 Realm，重点管理版本迁移和多线程读写锁。
>
> 数据一致性上遵循"单向数据更新源"原则：主页面优先展示本地缓存以秒开，同时后台拉取最新数据并更新本地数据库，再驱动 UI 刷新。关键状态如购物车、支付则引入乐观锁或事务回滚，防止本地与服务端偏离。

---

## 16：包体积和编译速度你怎么优化？

**口语答案：**

> 包体积我会先量化来源，看 Mach-O、资源、图片、动态库、Swift 标准库、无用代码分别占多少。图片可以压缩或改格式，无用资源要通过扫描和线上保护一起清理，重复库和重复资源也要处理。
>
> 二进制层面要谨慎，不能为了体积随便改编译参数。可以看符号裁剪、链接配置、架构切片、动态库数量，但每一项都要验证启动和兼容性。
>
> 编译速度则更多看模块边界、头文件依赖、预编译、增量编译命中率。Objective-C 项目尤其要控制公共头文件扩散；Swift 项目要注意泛型、类型推断和大文件对编译器的压力。

---

## 17：__weak 修饰符的底层实现原理是什么？

**口语答案：**

> __weak 的自动置空是由 Runtime 维护的全局弱引用表实现的。弱引用表是一个以对象内存地址为 Key、弱引用指针地址数组为 Value 的全局哈希表，位于 SideTables 结构中。
>
> 工作流程分三步：一是使用 __weak 时，Runtime 将指针地址存入对象的弱引用数组；二是对象释放执行 dealloc 时，Runtime 在弱引用表中找到对应记录；三是遍历所有弱引用指针赋值为 nil，最后移除记录。
>
> 性能上要注意：SideTables 是全局散列表，多线程并发读写会通过分段锁保证安全。频繁读写 weak 指针或大规模循环中访问 weak 会有加锁开销，极高频代码区应尽量避免滥用 weak。

---

## 18：你怎么提升 iOS 业务代码的可测试性？

**口语答案：**

> 可测试性首先来自边界设计。网络、数据库、时间、定位、支付这些外部依赖不要直接写死在业务对象里，而是通过协议或注入方式隔离，测试时才能替换成可控实现。
>
> 我会优先测试纯逻辑和高风险状态机，比如登录态、订单状态、缓存策略、权限判断。UI 测试成本高，可以覆盖核心链路，但不适合承担所有细节验证。
>
> 代码上要避免 ViewController 包办所有逻辑。只要业务判断能从 UIKit 生命周期里抽出来，就可以写窄测试。高级工程师要能用测试保护复杂规则，而不是只靠手工点页面。

---

## 19：RunLoop 的本质是什么？你在哪些场景真正需要关注它？

**口语答案：**

> RunLoop 本质就是事件循环，没有事件处理时休眠省电，有事件时唤醒处理。它管理着输入源、Timer、Observer 这几类东西。
>
> 我真正需要关注它的场景有几个：一是卡顿监控，通过 Observer 监听 RunLoop 各阶段的耗时，超阈值就抓主线程堆栈；二是 NSTimer 在列表滑动时失效，因为默认 Mode 在滑动时切到了 Tracking，Timer 不会触发；三是后台保活，申请 Background Task 后配合 RunLoop 维持任务执行；四是 AutoreleasePool 的释放时机，RunLoop 每轮结束时会 drain 池子。
>
> 日常开发不需要直接操作 RunLoop，但理解它才能讲清楚卡顿监控原理、Timer 失效原因、自动释放池时机这些高频面试追问。

---

# P2：加分题

## 20：Method Swizzling 的原理和风险是什么？

**口语答案：**

> Swizzling 是运行时交换两个方法的实现。本质是在类的方法列表里，把 selector 对应的 IMP 指针互换。
>
> 工程上适合做少量全局横切能力，比如埋点、兼容系统 Bug。但风险不小：交换顺序不可控，多方同时交换会互相影响；系统方法签名变化后容易出隐蔽问题。
>
> 我的原则是：能用显式协议、基类、组合解决的，就不优先用 Swizzling。非用不可时，必须 dispatch_once 保证只交换一次，交换前检查方法存在性，关键路径加回归验证。

---

## 21：Category 的加载机制是什么？+load 和 +initialize 有什么区别？

**口语答案：**

> Category 编译时生成分类结构，Runtime 启动阶段把分类方法合并到原类的方法列表里。所以 Category 能添加方法，但不能真正添加实例变量，因为对象内存布局在编译期已确定。关联对象可以模拟"加属性"，本质是全局关联表，不是对象里真的多了字段。
>
> +load 在 main 函数前被调用，每个类只调一次，Category 的 +load 也会执行，但顺序不可控。+initialize 是首次收到消息时才调用，且会被子类触发父类的实现。
>
> 所以 +initialize 里必须先判断 self 是不是当前类，否则父类逻辑会被执行多次。这在实际项目里是个高频坑。

---

## 22：AutoreleasePool 在什么场景下需要你主动关注？

**口语答案：**

> 日常 UIKit 应用里，主线程 RunLoop 每轮都有自动释放池，很多临时对象不需要手动处理。但在大量循环创建临时对象、图片处理、JSON 转换、后台线程任务里，如果不控制释放时机，内存峰值会明显升高。
>
> 我会在批量处理场景里人为缩小自动释放对象的生命周期，让每一小批任务结束后就释放临时对象，而不是等整个任务完成。
>
> 这题重点是理解"对象最终会释放"和"释放时机太晚导致峰值过高"是两回事。很多 OOM 问题不是泄漏，而是峰值控制失败。

---

## 23：如果让你接手一个历史包袱很重的 iOS 模块，你会怎么治理？

**口语答案：**

> 我不会一上来大重构。第一步是把模块现状量化清楚：崩溃、卡顿、启动耗时、核心链路失败率、代码依赖、测试缺口。先找最影响业务和稳定性的点。
>
> 第二步是建立边界。把网络、缓存、路由从 ViewController 里抽出稳定接口；对高风险逻辑补少量回归测试；新需求优先走新边界，旧逻辑逐步迁移。
>
> 第三步是控制风险。历史模块最怕"大改完一次性上线"，所以要分阶段、灰度、可回滚，并保持行为兼容。高级工程师的价值不是把代码改得漂亮，而是在业务不中断的情况下把风险慢慢降下来。

---

## 24：什么是 Tagged Pointer？它对 iOS 开发有什么影响？

**口语答案：**

> Tagged Pointer 是一种优化小对象的机制。像 NSNumber、NSDate 这类小对象，值本身很小，如果走正常的堆分配和引用计数管理，开销反而比数据本身还大。
>
> 所以系统把这些小对象的值直接编码进指针里，不走堆分配，也不参与引用计数。objc_msgSend 会先判断指针里是否是 Tagged Pointer，再决定要不要走正常的消息发送流程。
>
> 对开发的影响是：这类对象不走 dealloc，不要对它们做 retain/release 相关操作；调试时指针值看起来不像正常地址；在 64 位系统上这种优化更明显，因为指针空间足够大。

---

## 25：如果让你设计一个 iOS 端的热修复或动态化方案，你会考虑哪些因素？

**口语答案：**

> 我会从三个维度权衡：能力、风险、可维护性。
>
> 能力上，Runtime 提供了方法替换和消息转发的基础，OC 本身就是动态语言，这是天然优势。但动态化程度越高，安全风险越大——远程下发代码如果被篡改或出错，影响面比本地 Bug 大得多。
>
> 风险上要重点考虑苹果审核。纯运行时方法替换有一定灰区，动态库远程下发审核风险更高。所以要有回滚机制、灰度策略、异常监控，不能全量直接推。
>
> 我的原则是：能用配置化解决的不用代码下发，能本地热修的不动态加载。方案选型要和业务风险等级匹配，不是越"动态"越好。

---

# 架构篇：架构设计与工程治理

## 26：你怎么设计一个可维护的 MVVM 或类似分层架构？

**口语答案：**

> 我不会把 MVVM 理解成简单地多建一个 ViewModel 文件。真正的目标是让 ViewController 负责生命周期、渲染和绑定，ViewModel 负责纯逻辑和数据转换。网络、数据库等能力下沉到 Service 或 Repository。
>
> OC 项目里绑定可以用 KVO、RAC 或者 Block 回调实现。核心原则是单向：View 触发事件，ViewModel 处理并更新状态，View 观察状态变化后刷新。避免双向依赖导致状态零散、难以追踪。
>
> 避坑的关键在于限制 ViewModel 的职责：它不能引入视图层的强引用，避免变成 Massive ViewModel。复杂场景通过拆分小粒度的 Sub-ViewModel 或 UseCase 来解耦。

---

## 27：组件化和路由方案怎么设计，才能避免后期失控？

**口语答案：**

> 组件化的重点不是把代码拆成很多库，而是控制依赖方向和边界。业务模块之间不要互相直接 import，公共能力下沉到基础层，跨模块调用通过协议、路由或服务注册暴露稳定接口。
>
> 路由设计我会区分页面跳转和服务调用。页面跳转可以用 URL 或统一 Route 对象承载参数；服务调用更适合协议化，返回明确类型，避免全靠字符串和字典。参数校验、降级页、登录拦截也要放在路由体系里统一处理。
>
> 失控通常来自三点：路由字符串散落、模块间偷懒直接依赖、公共层不断膨胀。我的做法是让每个模块只暴露必要接口，用工具或编译规则约束依赖方向。

---

# 面试表达建议

1. **套用框架**：一句话结论 → 核心原理 → 我怎么做 → 风险/取舍。
2. 每题控制在 60 到 90 秒，不要一开始就陷入源码细节。
3. 少背 API 名字，多讲取舍、风险、验证方式。
4. 如果没有真实项目经验，不要硬编，可以说"我理解的处理方式是"，但不要伪造成线上案例。
5. 高级面试更看重定位问题和治理复杂度的能力，不只是知道某个方法怎么调用。
