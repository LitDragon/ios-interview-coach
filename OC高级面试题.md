# OC与通用工程高级面试题排序版

> 目标：按高级 iOS 面试中的重要性和区分度排序。答案采用面对面口语表达，每题建议控制在 60 到 90 秒。
>
> 筛选原则：删除单纯 API 记忆、代码默写、低阶概念题；把必要基础合并到更高级的问题里回答。

---

## 题目分级

| 优先级 | 定位 | 题量 |
| --- | --- | --- |
| P0 | 必须讲清楚，决定高级工程师基本面 | 12 题 |
| P1 | 高频进阶题，体现项目深度 | 8 题 |
| P2 | 加分题，体现边界经验和工程判断 | 5 题 |

## 已删除或合并的低价值题

- “ARC 和 MRC 区别”“strong、weak、copy、assign 区别”不再单独成题，合并到内存管理和泄漏治理里。
- “dispatch_once 原理”“队列类型表格背诵”不再单独成题，合并到并发设计和线程安全里。
- “KVC 基础用法”删除，只保留 KVO 原理和风险。
- “圆角代码方案”改成渲染链路和真实优化取舍，不再背代码。
- “MVC、MVP、MVVM、VIPER 枚举”改成架构落地题，重点看职责边界和可维护性。

---

# P0：高级 iOS 必会题

## 01：你怎么理解 iOS 的内存管理？ARC 到底解决了什么，没有解决什么？

**口语答案：**

> ARC 本质上还是引用计数，不是垃圾回收。它解决的是 retain、release 这些所有权操作由人工维护容易出错的问题，编译器会根据对象的生命周期，在合适的位置插入持有和释放逻辑。
>
> 但 ARC 没有解决所有内存问题。比如循环引用、缓存无限增长、大图解码占用、C 指针或 Core Foundation 对象管理，这些仍然需要工程师自己判断。weak 的自动置空也依赖运行时维护的弱引用表，并不是简单地“不持有”这么一句话。
>
> 面试里我会强调：ARC 让普通对象生命周期更安全，但高级开发仍然要能判断谁拥有谁、对象什么时候释放、异步回调里会不会延长生命周期，以及是否有非 Objective-C 对象需要手动桥接和释放。

---

## 02：项目里出现内存泄漏，你通常怎么定位和修复？

**口语答案：**

> 我会先确认是不是持续增长，不能只看某一瞬间的内存峰值。比如图片列表快速滑动时内存上升是正常的，但退出页面后对象还留着，就要重点看引用链。
>
> 在研发阶段，我们主要依赖自动化检测拦截：比如在 Debug 阶段或 CI 流程中集成 MLeaksFinder 和 FBRetainCycleDetector，自动发现并弹窗提示内存泄漏与强引用环。对于复杂的长链路，我再配合 Xcode Memory Graph 或 Instruments 的 Leaks 进行堆栈回溯。
>
> 如果是线上 OOM（内存溢出）治理，由于 OOM 发生时通常没有崩溃堆栈，我们会监控系统的 Footprint 物理内存，并对大内存对象、未限额的缓存、大图解码进行专项治理，或在后台退出时主动释放资源。
>
> 修复时不会只机械地加 weak，而是要理清所有权关系，彻底打断强引用环，并对短生命周期任务添加防护，避免对象提前释放。

---

## 03：Block 捕获变量和循环引用，你会怎么给面试官讲？

**口语答案：**

> 我会先说结论：Block 是对象，逃逸后通常会被拷贝到堆上；它捕获外部变量时，会根据变量类型产生不同的持有或拷贝行为。普通局部变量更像是捕获当时的值，对象变量会涉及强引用，__block 变量会被包装成一个可共享的结构。
>
> 循环引用的核心不是“Block 一定会循环”，而是“谁持有 Block，Block 又是否强持有谁”。比如控制器持有一个网络回调 Block，Block 里直接使用 self，就可能形成控制器到 Block、Block 到控制器的闭环。
>
> 我的处理习惯是：需要避免闭环时用 weak 打断；进入回调后如果需要保证执行期间 self 不释放，再临时转强引用。更重要的是判断业务语义，有些短生命周期的同步 Block 不需要过度 weak，否则反而可能导致关键逻辑执行一半对象消失。

---

## 04：你怎么设计线程安全？GCD、锁、OperationQueue 怎么取舍？

**口语答案：**

> 我会先区分两类问题：一类是把耗时任务移出主线程，另一类是共享状态的线程安全。前者用 GCD 或 OperationQueue 都能解决；后者要明确保护的是哪段临界区，不能只说“异步一下”。
>
> GCD 适合简单任务调度，比如异步 IO、回主线程刷新 UI、用串行队列保护资源。OperationQueue 更适合有依赖、取消、最大并发数、任务状态管理的场景。锁适合保护非常小的同步临界区，比如 os_unfair_lock、递归锁、读写锁，各自都有适用边界。
>
> 我会特别避免几个坑：不要在主队列同步派发到主队列；不要用 atomic 误以为对象整体线程安全；不要把大任务放在锁里；也不要让多个队列交叉等待。高级一点的做法是尽量减少共享可变状态，让数据流单向，必要时再用队列或锁做明确保护。

---

## 05：RunLoop 在 iOS 里解决了什么问题？它和卡顿、Timer 有什么关系？

**口语答案：**

> RunLoop 可以理解为线程的事件循环。主线程不是执行完就退出，而是通过 RunLoop 持续接收触摸、Timer、Source、布局和渲染相关事件，所以 UI 应用才能一直响应用户操作。
>
> Timer 依赖 RunLoop，如果主线程正在执行耗时任务，Timer 就不会准时触发；如果 Timer 加在默认模式，滚动时 RunLoop 切到 tracking 模式，Timer 也可能暂停。这就是为什么很多滚动场景下计时器表现不稳定。
>
> 卡顿监控也会用到 RunLoop。主线程长时间停在某个阶段，通常说明有同步 IO、复杂布局、大量计算或锁等待。实际排查时我不会只看 RunLoop 状态，而是要抓主线程堆栈，结合 Time Profiler 找到真正耗时的方法。

---

## 06：什么是卡顿？你怎么监控、定位和优化？

**口语答案：**

> 卡顿本质是主线程没有在一帧预算内完成事件处理、布局、提交和渲染相关工作。60Hz 屏幕大约是 16.7 毫秒一帧，120Hz 会更严格，所以不能只用一个固定阈值理解所有设备。
>
> 监控上，线下我会用 Instruments 的 Time Profiler、Core Animation、主线程检查工具；线上可以通过 RunLoop 观察或子线程 watchdog 抓取主线程堆栈，但要控制采样成本，避免监控本身影响性能。
>
> 优化时先看证据，不会直接猜。常见方向包括：主线程同步 IO 或 JSON 解析、Auto Layout 约束过多、列表高度反复计算、图片解码在主线程、锁竞争、频繁创建对象。优化后要用同一场景复测帧率、耗时和主线程堆栈，确认不是把问题转移到别处。

---

## 07：启动优化你会从哪些阶段下手？

**口语答案：**

> 我会把启动分成 main 函数前和 main 函数后。main 前主要是系统加载可执行文件、动态库、符号绑定、Objective-C 类和 Category 注册、load 方法执行。这里的优化重点是减少动态库数量、控制 +load、减少不必要的全局初始化。
>
> main 后是业务启动链路，包括 AppDelegate、首屏路由、配置读取、网络预热、数据库初始化、埋点 SDK 初始化等。这里要区分首屏必要任务和可延后任务，能懒加载的不要阻塞首屏，能异步的不要占主线程。
>
> 我通常会先建立启动耗时分段指标，再看 Time Profiler 和 dyld 统计。高级面试里不能只说“异步化”，因为异步化可能引入时序问题。更稳的做法是按依赖关系拆启动任务，保证首屏可用，同时让非关键 SDK 在合适时机初始化。

---

## 08：复杂列表或首页滑动不流畅，你会怎么优化？

**口语答案：**

> 列表优化我会从数据、布局、渲染、图片四个方向看。数据层要避免滚动中同步请求和大量主线程解析；布局层要减少重复计算，必要时缓存高度或提前生成展示模型；渲染层要减少无意义层级、透明混合和阴影裁剪；图片层要控制尺寸、解码时机和缓存策略。
>
> Cell 复用只是基础，高级场景更容易出问题的是复用状态污染、异步图片回调错位、预取和取消不完整、复杂富文本或自适应布局在主线程反复计算。
>
> 我实际处理时会先复现稳定场景，打开帧率和主线程耗时工具，看是 CPU 计算、GPU 合成还是 IO 阻塞。优化后要验证快速滑动、反复进入退出、低端设备和弱网场景，避免只在开发机上看起来流畅。

---

## 09：Runtime 的消息发送机制是什么？为什么它对高级 iOS 很重要？

**口语答案：**

> Objective-C 的方法调用本质是消息发送。对象收到消息后，会先根据 isa 找到类，再从方法缓存和方法列表里找 selector 对应的实现，找不到就沿继承链继续找。
>
> 如果最终还是找不到，并不是马上崩溃，Runtime 会给几次补救机会：动态方法解析、快速转发、完整消息转发。全部处理不了，才会出现 unrecognized selector。
>
> 这件事重要，是因为很多高级能力都建立在 Runtime 上，比如 KVO 的动态子类、Method Swizzling、Category 方法加载、埋点、热修复思路、消息转发型路由。面试里我会重点讲清楚“动态性带来能力，也带来风险”，不能只背 objc_msgSend 名字。

---

## 10：你怎么做一个可维护的 MVVM 或类似分层架构？

**口语答案：**

> 我不会把 MVVM 理解成简单地多建一个 ViewModel 文件。真正的目标是让 ViewController 负责生命周期、渲染和绑定，ViewModel 负责纯逻辑和数据转换。网络、数据库等能力下沉到 Service 或 Repository。
>
> 在现代 iOS 开发中，MVVM 常配合 SwiftUI / Combine。为解决传统双向绑定导致的状态零散、难以追踪的痛点，我会引入单向数据流（Unidirectional Data Flow）思想，即：View 触发 Action -> ViewModel 统一变更 State -> View 观察 State 并刷新。这样副作用能被集中管控，逻辑非常利于进行单元测试。
>
> 避坑的关键在于限制 ViewModel 的职责：它绝对不能引入 UIKit/SwiftUI 视图层的强引用，避免变成 Massive ViewModel。对复杂的业务场景，通过拆分小粒度的 Sub-ViewModel 或 UseCase 来解耦。

---

## 11：组件化和路由方案怎么设计，才能避免后期失控？

**口语答案：**

> 组件化的重点不是把代码拆成很多库，而是控制依赖方向和边界。业务模块之间不要互相直接 import，公共能力下沉到基础层，跨模块调用通过协议、路由或服务注册暴露稳定接口。
>
> 路由设计我会区分页面跳转和服务调用。页面跳转可以用 URL 或统一 Route 对象承载参数；服务调用更适合协议化，返回明确类型，避免全靠字符串和字典。参数校验、降级页、登录拦截、埋点和灰度也要放在路由体系里统一处理。
>
> 失控通常来自三点：路由字符串散落、模块间偷懒直接依赖、公共层不断膨胀。我的做法是让每个模块只暴露必要接口，用工具或编译规则约束依赖方向，并且把跨模块协议当成产品级 API 维护。

---

## 12：线上 Crash 你怎么排查？怎么把稳定性做成体系？

**口语答案：**

> 我会先看 Crash 是否符号化完整，再按影响面、版本、设备、系统、页面路径聚类。单个堆栈不能直接下结论，要结合用户行为、日志、埋点、最近发布 diff 和异常上下文。
>
> 常见 Crash 类型包括数组越界、字典插入 nil、野指针、KVO 或通知释放问题、主线程 UI 违规、并发读写、unrecognized selector、OOM。不同类型排查方式不一样，比如 OOM 通常没有标准崩溃堆栈，需要看内存峰值、页面路径和大对象分配。
>
> 体系化上，我会做崩溃率、卡死率、OOM、启动失败、关键路径错误率这些指标，并且给高风险模块加防御和灰度。高级工程师不能只会修一个 Crash，还要能降低同类问题再次出现的概率。

---

# P1：高频进阶题

## 13：KVO 的底层原理是什么？实际项目里有哪些风险？

**口语答案：**

> KVO 的核心是运行时动态子类。对象被观察后，系统会生成一个中间子类，把对象的 isa 指过去，并重写被观察属性的 setter，在赋值前后插入通知逻辑。
>
> 所以 KVO 对调用路径有要求：通过 setter 修改才能自动触发，直接改成员变量不会触发；可变集合内部变化也不一定符合预期。它还有重复添加、移除时机、上下文区分、线程回调位置这些风险。
>
> 我的使用原则是：简单状态观察可以用，但要封装观察生命周期，使用明确的 context，避免在大规模对象上滥用。现代项目里，如果已有响应式框架、Combine、Swift Observation 或明确的回调链路，我会优先选择更可控的方案。

---

## 14：离屏渲染是什么？圆角、阴影、mask 应该怎么优化？

**口语答案：**

> 离屏渲染分为 GPU 离屏（GPU 切换上下文）和 CPU 离屏（Core Graphics 自定义绘制）。优化核心在于减少不必要的渲染开销。
> 1. **避免 GPU 离屏**：对于圆角，优先通过 UI 设计侧切好带圆角的图片，或在后台用 `UIGraphicsImageRenderer` 生成圆角图片；阴影必须指定 `shadowPath`，明确形状后 GPU 不需要对图层纹理进行实时遮罩计算。
> 2. **按需使用光栅化**：`shouldRasterize` 仅适用于复杂但静态的视图，对于列表滚动等频繁刷新的 UI，它会导致缓存命中率极低且产生额外内存开销，反而加剧卡顿。
> 3. **工具验证**：使用 Instruments 中的 Core Animation 工具开启 "Color Offscreen-Rendered Yellow"，在模拟器或真机上对列表滑动进行全场景验证，确保关键路径无黄色高亮。

---

## 15：大图、图片缓存和内存峰值你怎么处理？

**口语答案：**

> 图片问题不能只看文件大小，更要看解码后的内存占用。比如一张压缩后几百 KB 的图片，按像素展开后可能占几十 MB，如果在主线程解码，还会同时造成内存峰值和卡顿。
>
> 我的处理方式是按展示尺寸请求或裁剪图片，避免加载远大于屏幕显示尺寸的原图；下载后尽量异步解码；内存缓存要有数量和成本限制；收到内存警告或进入后台时及时释放可重建缓存。
>
> 在列表里还要处理复用和取消。Cell 复用后旧请求必须取消或校验标识，否则会出现图片错位。预加载也要有边界，弱网和快速滑动时，盲目预取会让网络、CPU、内存一起恶化。

---

## 16：网络层你会怎么设计，才能支撑大型业务？

**口语答案：**

> 一个成熟的网络层，要具备高可用性和可观测性。在架构上我采用“业务层-适配层-传输层”的三层解耦设计，业务层只感知强类型 Model，底层统一处理公共 Header 注入、Token 刷新和统一错误码映射。
>
> 针对大厂和大型业务场景，我会在网络层落地两项核心优化：
> 1. **域名防劫持与解析优化**：引入 **HTTPDNS** 绕过运营商局部 DNS 劫持，同时利用本地缓存域名解析结果，大幅降低首包时间（TTFB）。
> 2. **弱网与建连优化**：推动 **HTTP/3 (QUIC)** 协议落地，利用其单向握手机制加速建连，并利用其无队头阻塞的多路复用特性，在电梯、地铁等高丢包弱网环境下提升数据传输成功率。
>
> 另外，必须建立完善的网络 APM 打点，对各阶段（DNS解析、TCP握手、SSL握手、响应传输）耗时进行监控，以便精准定位网络瓶颈。

---

## 17：本地缓存和数据一致性你怎么做？

**口语答案：**

> 我会先按照数据价值和一致性要求进行分层设计：
> 1. **临时展示与状态数据**：使用 **MMKV** 代替传统的 `UserDefaults`。因为 MMKV 底层通过 `mmap`（内存映射）将文件直接映射到用户空间，实现了**零拷贝（Zero-Copy）**，在读写速度上比传统的 Plist/UserDefaults 发生 I/O 阻塞快了几个数量级，并且支持多进程/多线程并发安全。
> 2. **复杂关系型数据**：使用 SQLite 或 Realm，重点管理数据库的版本迁移和多线程写入时的读写锁问题。
>
> 在数据一致性上，我会遵循“单向数据更新源”原则：主页面优先展示本地缓存以秒开，同时后台拉取最新的网络数据并更新本地数据库，再驱动 UI 刷新。如果涉及购物车、支付等关键状态，则必须引入“乐观锁”或事务回滚机制，防止本地与服务端产生数据偏离。

---

## 18：包体积和编译速度你怎么优化？

**口语答案：**

> 包体积我会先量化来源，看 Mach-O、资源、图片、动态库、Swift 标准库、无用代码分别占多少。图片可以压缩或改格式，无用资源要通过扫描和线上保护一起清理，重复库和重复资源也要处理。
>
> 二进制层面要谨慎，不能为了体积随便改编译参数。可以看符号裁剪、链接配置、架构切片、动态库数量，但每一项都要验证启动和兼容性。
>
> 编译速度则更多看模块边界、头文件依赖、预编译、增量编译命中率和依赖管理。Objective-C 项目尤其要控制公共头文件扩散；Swift 项目要注意泛型、类型推断和大文件对编译器的压力。

---

## 19：__weak 修饰符的底层实现原理是什么？当对象释放时，它是怎么自动置空的？

**口语答案：**

> `__weak` 的自动置空是由 Objective-C Runtime 维护的全局弱引用表（Weak Table）实现的。弱引用表本质上是一个以对象内存地址为 Key、弱引用指针地址的数组为 Value 的全局哈希表（具体位于 `SideTables` 结构中）。
>
> **具体的工作流程分为三个主要步骤：**
> 1. **初始化与注册**：当使用 `__weak` 修饰一个指针时，Runtime 会调用 `objc_initWeak`，底层通过 `objc_storeWeak` 算出哈希桶，将当前指针的地址（`objc_object**`）存入对应对象的弱引用数组中。
> 2. **析构清理触发**：当被引用的对象释放（执行 `dealloc`）时，Runtime 会依次调用 `_objc_rootDealloc` -> `object_dispose` -> `objc_destructInstance` -> `clearDeallocating`。
> 3. **指针清空并移除**：在 `clearDeallocating` 中，Runtime 依据对象地址在弱引用表中找到对应记录，遍历该记录下注册的所有弱引用指针并将其全部赋值为 `nil`，最后将该记录从哈希表中移除。
>
> 在大厂面试中我会补充一个性能考点：因为 `SideTables` 是一个全局的散列表，多线程并发读写弱引用指针时，会通过分段锁（SpinLock/Mutex）来保证安全。这意味着频繁地读写 weak 指针或在大规模循环中访问 weak，会带来一定的加锁开销和哈希冲突。因此，极高频调用的代码区或核心循环体中应尽量避免滥用 weak 属性。

---

## 20：你怎么提升 iOS 业务代码的可测试性？

**口语答案：**

> 可测试性首先来自边界设计。网络、数据库、时间、定位、支付这些外部依赖不要直接写死在业务对象里，而是通过协议或注入方式隔离，这样测试时才能替换成可控实现。
>
> 我会优先测试纯逻辑和高风险状态机，比如登录态、订单状态、缓存策略、权限判断、路由参数解析。UI 测试成本高，可以覆盖核心链路，但不适合承担所有细节验证。
>
> 代码上要避免 ViewController 包办所有逻辑。只要业务判断能从 UIKit 生命周期里抽出来，就可以写窄测试。高级工程师要能用测试保护复杂规则，而不是只靠手工点页面。

---

# P2：加分题

## 21：Method Swizzling 你怎么看？什么时候该用，什么时候不该用？

**口语答案：**

> Swizzling 是运行时交换方法实现，能力很强，但也很危险。它适合做少量全局横切能力，比如埋点、兼容系统问题、统一生命周期观察；不适合当成普通业务扩展手段。
>
> 风险主要有三个：交换顺序不可控，多方同时交换同一方法会互相影响；父类方法和子类方法处理不好会扩大影响面；系统方法签名或行为变化后容易出现隐蔽问题。
>
> 如果必须用，我会保证只初始化一次，交换前检查方法是否存在，命名避免冲突，尽量集中管理，并且给关键路径加回归验证。能用显式协议、基类、组合或系统回调解决的，就不要优先用 Swizzling。

---

## 22：Category 的底层原理是什么？为什么不能真正添加实例变量？

**口语答案：**

> Category 编译后会形成一份分类信息，里面包含方法、协议、属性声明等。运行时加载类和分类时，会把分类方法合并到原类的方法列表里，所以分类可以给已有类扩展方法。
>
> 但对象的实例变量布局在编译期就已经确定了。Category 是运行时合并的，如果允许随便添加实例变量，就会改变对象内存布局，影响已有实例和子类，所以它不能真正新增 ivar。
>
> 关联对象可以模拟“给分类加属性”，但本质是运行时维护了一张关联表，不是对象内存里真的多了字段。它适合少量扩展状态，不适合承载复杂业务模型，也要注意关联策略和生命周期。

---

## 23：AutoreleasePool 在什么场景下需要你主动关注？

**口语答案：**

> 日常 UIKit 应用里，主线程 RunLoop 每轮都会有自动释放池，所以很多临时对象不需要手动处理。但在大量循环创建临时对象、图片处理、JSON 转换、后台线程任务里，如果不控制释放时机，内存峰值会明显升高。
>
> 我会在批量处理场景里人为缩小自动释放对象的生命周期，让每一小批任务结束后就释放临时对象，而不是等整个任务完成。后台线程如果自己创建长生命周期任务，也要关注自动释放池是否存在。
>
> 这题重点不是背 autorelease，而是能说明“对象最终会释放”和“释放时机太晚导致峰值过高”是两回事。很多 OOM 问题不是泄漏，而是峰值控制失败。

---

## 24：通知、代理、Block、响应式绑定，你怎么选？

**口语答案：**

> 我会按通信关系选择。代理适合一对一、需要明确回调契约的场景，比如子页面把用户操作告诉上层。Block 适合轻量回调，但要注意生命周期和循环引用。通知适合一对多广播，但不适合承载复杂业务流程。
>
> 响应式绑定适合状态驱动 UI，尤其是多个状态组合出一个展示结果时更清晰。但它也会让调用链变隐式，调试成本更高，所以要有命名规范和生命周期管理。
>
> 我的原则是：局部明确用代理或 Block，全局事件慎用通知，复杂状态用可观察模型但保持单向数据流。通信方式不是越高级越好，关键是让依赖关系可读、可控、可测试。

---

## 25：如果让你接手一个历史包袱很重的 iOS 模块，你会怎么治理？

**口语答案：**

> 我不会一上来大重构。第一步是把模块现状量化清楚：崩溃、卡顿、启动耗时、核心链路失败率、代码依赖、页面职责和测试缺口。先找最影响业务和稳定性的点。
>
> 第二步是建立边界。比如把网络、缓存、路由、埋点从 ViewController 里抽出稳定接口；对高风险逻辑补少量回归测试；新需求优先走新边界，旧逻辑逐步迁移。
>
> 第三步是控制风险。历史模块最怕“大改完一次性上线”，所以我会分阶段、灰度、可回滚，并保持行为兼容。高级工程师的价值不是把代码改得漂亮，而是在业务不中断的情况下把风险慢慢降下来。

---

# 面试表达建议

1. 先给结论，再讲原理，最后补项目经验。
2. 每题控制在 60 到 90 秒，不要一开始就陷入源码细节。
3. 少背 API 名字，多讲取舍、风险、验证方式。
4. 如果没有真实项目经验，不要硬编，可以说“我理解的处理方式是”，但不要伪造成线上案例。
5. 高级面试更看重定位问题和治理复杂度的能力，不只是知道某个方法怎么调用。
