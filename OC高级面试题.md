# OC与通用工程高级面试题排序版

> 目标：按高级 iOS 面试中的重要性和区分度排序。答案采用面对面口语表达，每题建议控制在 60 到 90 秒。
>
> 筛选原则：删除单纯 API 记忆、代码默写、低阶概念题；把必要基础合并到更高级的问题里回答。

---

## 回答结构模板

每题套用以下框架，即使记不全细节也能组织出完整回答：

1. **一句话结论**："这个机制本质是..."
2. **核心原理**：2-3 句，讲清流程
3. **我怎么做**：1-2 句，讲实际处理
4. **风险/取舍**：1 句，体现工程判断

---

## 题目分级

| 优先级 | 定位 | 题量 |
| --- | --- | --- |
| P0 | 必须讲清楚，决定高级工程师基本面 | 10 题 |
| P1 | 高频进阶题，体现项目深度 | 9 题 |
| P2 | 加分题，体现边界经验和工程判断 | 6 题 |
| 架构篇 | 架构设计与工程治理，按需考察 | 2 题 |

---

# P0：高级 iOS 必会题

## 01：你怎么理解 iOS 的内存管理？ARC 到底解决了什么，没有解决什么？

**口语答案：**

> ARC 本质上还是引用计数，不是垃圾回收。它解决的是人工调用 retain/release 容易出错的问题，由编译器在合适位置自动插入持有和释放代码。
>
> 但它不能解决所有内存问题。比如两个对象循环引用、强持有的 Timer 无法销毁、缓存无限增长导致 OOM，以及 C 指针或 Core Foundation 对象的内存，这些仍需手动管理。
>
> 总结来说，ARC 保证了普通对象的基础安全，但高级开发必须时刻清楚谁持有谁、对象什么时候销毁，以及怎么打断循环引用。

---

## 02：项目里出现内存泄漏，你通常怎么定位和修复？

**口语答案：**

> 排查泄漏我主要看页面退出后，对象有没有被释放。
>
> **开发阶段**，我们主要靠工具自动拦截：在 Debug 模式下集成 `MLeaksFinder` 和 `FBRetainCycleDetector`，一旦有内存泄漏或强引用环会自动弹窗。
> **排查复杂链路时**，我会用 Xcode 的 `Memory Graph` 查引用关系树，或者用 Instruments 的 `Leaks` 抓泄漏堆栈。
> **修复的核心**是理清所有权，用 `weak` 彻底打断强引用环。
> 如果是线上 **OOM（内存溢出）** 治理，我们会通过 APM 监控 `Memory Footprint` 物理内存，限制图片和缓存的大小，防止峰值突增。

---

## 03：Block 捕获变量和循环引用，你会怎么给面试官讲？

**口语答案：**

> Block 本质是 OC 对象，逃逸后通常会被拷贝到堆上。
>
> **它捕获变量的规则是：**
> - 局部基本变量：捕获的是当时的值。
> - 局部对象变量：强引用持有，会导致引用计数加1。
> - `__block` 变量：会被包装成一个结构体对象，捕获的是结构体指针，因此在 Block 内部可以修改该变量的值。
>
> **循环引用**是“A强持有B，B又强持有A”形成了闭环。比如控制器持有网络回调 Block，Block 里直接写 `self`。
> 我们的做法是：在 Block 外部声明 `__weak typeof(self) weakSelf = self` 打断闭环。如果需要保证回调执行期间 self 不被中途释放，可以在 Block 内部临时转强 `__strong typeof(weakSelf) strongSelf = weakSelf`。

---

## 04：你怎么设计线程安全？GCD、锁、OperationQueue 怎么取舍？

**口语答案：**

> 线程安全主要分两类：一是耗时任务异步化，避免阻塞主线程；二是共享状态的防并发读写。
>
> **工具取舍上：**
> - **GCD**：最常用，适合简单的异步调度、回主线程刷新 UI，或者用串行队列保护单一资源。
> - **OperationQueue**：基于 GCD 封装，适合有依赖关系、需要支持取消任务、或者需要限制最大并发数的复杂场景。
> - **同步锁**：用来保护细粒度的临界区。日常保护属性优先用 `os_unfair_lock`（性能最高）；有递归调用风险用 `NSRecursiveLock`；读多写少用读写锁（`pthread_rwlock`）。
>
> **防坑指南**：不要在大任务里加锁；不要在主队列同步派发任务（会导致死锁）；多用单向数据流减少共享状态。

---

## 05：什么是卡顿？你怎么监控、定位和优化？

**口语答案：**

> 卡顿本质上是主线程没有在一帧预算内（60Hz 约 16.7ms，120Hz 约 8.3ms）完成布局、提交和渲染，导致屏幕掉帧。
>
> **监控与排查：**
> - **线下**：用 Instruments 的 `Time Profiler` 查看 CPU 耗时函数，或者用 `Core Animation` 查看 GPU 提交情况。
> - **线上**：在子线程建立 `Watchdog`，当发现主线程 RunLoop 在特定状态停留超过阈值（如 2-3 秒），就判定为卡顿并抓取此时的主线程堆栈上传。
>
> **优化方向：**
> 常见瓶颈在主线程，优化手段包括：把复杂的 JSON 解析和磁盘 I/O 移到子线程；缓存 TableView 高度避免重复计算；控制图层层级，减少不必要的离屏渲染。

---

## 06：启动优化你会从哪些阶段下手？

**口语答案：**

> 启动优化主要分为 **main 函数前（pre-main）** 和 **main 函数后（post-main）** 两个阶段：
>
> **1. pre-main 阶段（系统加载）**：
> 主要是系统加载动态库、注册类、执行 `+load` 方法。优化点是：尽量减少非必要的自定义动态库（建议合并或转静态库）；清理无用的类和方法；严禁在 `+load` 中做耗时操作。
>
> **2. post-main 阶段（业务初始化）**：
> 从 `didFinishLaunching` 到首屏渲染。优化点是：采用“任务分级拆解”，非首屏必须的 SDK 延迟加载或异步初始化；首屏数据优先使用本地缓存“秒开”，然后再网络请求更新。
>
> 治理时，我们会使用 dyld 的环境变量输出和 Time Profiler 来测定各段耗时，确保首屏加载速度。

---

## 07：复杂列表或首页滑动不流畅，你会怎么优化？

**口语答案：**

> 列表滑动的流畅度治理，我主要从以下四个维度优化：
>
> 1. **数据层**：避免在滚动时进行同步的数据解析或复杂的格式化操作。
> 2. **布局层**：在后台线程提前计算好 Cell 子控件的 frame 和高度，并与 Model 绑定。滚动时直接使用缓存高度，避免高频调用 `heightForRow` 重新计算布局。
> 3. **渲染层**：减少图层嵌套和半透明混合，避免离屏渲染。
> 4. **图片层**：大图在后台异步解码后再提交给 UIImageView，控制图片展示尺寸防止内存爆涨，同时滚动时取消不必要的图片下载任务。

---

## 08：Runtime 的消息发送机制是什么？为什么它对高级 iOS 很重要？

**口语答案：**

> Objective-C 的方法调用本质是消息发送，即调用 `objc_msgSend`。
>
> **它的查找与补救流程如下：**
> 1. **消息发送阶段**：首先快速查找当前类的 `cache`（方法缓存），找不到就去类及父类的 `method_list` 中查找。
> 2. **动态方法解析**：如果沿继承链都找不到，会调用 `resolveInstanceMethod:`，给你一次动态添加方法实现（IMP）的机会。
> 3. **消息转发**：如果未添加，会进入 `forwardingTargetForSelector:` 让你指定备用接收者；最后走 `forwardInvocation:` 进行完整转发。如果全失败，则奔溃报错。
>
> **为什么重要**：因为 iOS 很多高阶能力，比如 `KVO` 动态子类生成、`Method Swizzling` 埋点热修、消息转发型组件路由，都基于这个动态查找链条。

---

## 09：Objective-C 对象的内存结构是什么？isa 指针起什么作用？

**口语答案：**

> **内存结构**：OC 对象在堆内存中的首个字段是 `isa` 指针，占用 8 字节，后面紧随其属性数据。
>
> **isa 的作用与指向链**：
> - **实例对象**的 `isa` 指向它的 **类对象（Class）**。类对象中存储了实例方法列表、属性和协议。
> - **类对象**也是个对象，它的 `isa` 指向它的 **元类（Meta Class）**。元类中存储了类方法列表。
> - **元类**的 `isa` 最终指向根元类，根元类指向自身。
>
> 通过这套 `isa` 和 `superclass` 构成的继承链，Runtime 才能在收到消息时，顺着指针找到对应的方法实现。

---

## 10：线上 Crash 你怎么排查？怎么把稳定性做成体系？

**口语答案：**

> 排查 Crash 的核心是获取**符号化完整**的堆栈，并结合用户路径和日志上下文进行归类。
>
> **排查思路：**
> - 数组越界、字典赋 nil、unrecognized selector 等直接看堆栈定位代码。
> - 野指针（已释放对象响应消息）使用 `Malloc Scribble` 线下复现。
> - OOM（内存溢出）由于没有崩溃堆栈，需要通过监控 `Memory Footprint` 并记录物理内存上限和用户路径来判定。
>
> **体系化建设：**
> 线上引入 APM 监控崩溃率和卡死率。建立开发规范，在核心逻辑（如序列化、底层网络）中增加兜底防御；在灰度发布阶段，设定崩溃率报警阈值，出现异常拦截。

---

# P1：高频进阶题

## 11：KVO 的底层原理是什么？实际项目里有哪些风险？

**口语答案：**

> KVO 底层采用了 **isa-swizzling（指针交换）** 技术。
> 
> 当你监听一个对象的属性时，Runtime 会动态创建一个子类，类名是 `NSKVONotifying_原类名`。然后将该对象的 `isa` 指针修改为指向这个中间子类。
> 
> 中间子类重写了被观察属性的 `setter` 方法，在真实赋值前后分别插入 `willChangeValueForKey:` 和 `didChangeValueForKey:`，从而触发观察者的回调。
> 
> **风险**：必须手动移除观察者，否则可能发生野指针 Crash（iOS 11后有优化但仍建议显式移除）；重复添加或移除会报错；直接修改成员变量（如 `_value = x`）不会触发 KVO，必须通过 setter 触发。

---

## 12：离屏渲染是什么？圆角、阴影、mask 应该怎么优化？

**口语答案：**

> 离屏渲染主要分两种：
> 1. **GPU 离屏渲染**：GPU 在当前屏幕帧缓冲区外，开辟临时缓冲区进行混合渲染。这会造成频繁的上下文切换（Context Switch），开销极大。
> 2. **CPU 离屏渲染**：使用 Core Graphics（如 `drawRect:`）在后台线程进行异步绘制，开销主要在 CPU 算力和物理内存上。
> 
> **优化方案：**
> - **圆角**：若需要裁剪，优先让设计切好圆角图，或者后台绘制好图片，避免同时开启 `cornerRadius` 和 `masksToBounds`。
> - **阴影**：必须设定显式的 `shadowPath`，避免 GPU 实时探索图层轮廓。
> - **光栅化（`shouldRasterize`）**：仅适用于复杂但完全静态的图层。若图层频繁刷新，开启它反而会导致离屏缓存频繁失效，恶化卡顿。

---

## 13：大图、图片缓存和内存峰值你怎么处理？

**口语答案：**

> 图片的物理内存占用只取决于 **像素大小**（宽 × 高 × 4 字节），而非文件体积。一张几百 KB 的大图解码后可能占用几十 MB 内存。
> 
> **处理手段：**
> 1. **下采样（Downsampling）**：根据 ImageView 的实际尺寸对图片进行缩略读取和解码，避免加载完整大图。
> 2. **异步解码**：利用后台线程对图片进行解码，避免滚动到屏幕内时在主线程解码造成卡顿。
> 3. **缓存限额**：限制内存缓存（如 NSCache）的最大成本和数量，在收到内存警告或 App 退到后台时，主动清空缓存。

---

## 14：网络层你会怎么设计，才能支撑大型业务？

**口语答案：**

> 一个高可用的网络层，我采用“业务层 - 适配层 - 传输层”三层解耦架构，业务层只感知强类型 Model，底层统一处理 Header 注入、Token 刷新和错误码映射。
> 
> **大厂工程核心优化：**
> 1. **引入 HTTPDNS**：绕过运营商 Local DNS 劫持，使用 IP 直接建连，并本地缓存解析结果，大幅降低首包时间（TTFB）。
> 2. **拥抱 HTTP/3 (QUIC)**：在弱网环境（如地铁、电梯）下，利用其单向握手极速建连和无队头阻塞的多路复用特性，提升接口成功率。
> 3. **建立网络 APM 监控**：对 DNS 解析、TCP 握手、SSL 握手、数据传输各阶段进行打点监控，精准定位耗时瓶颈。

---

## 15：本地缓存和数据一致性你怎么做？

**口语答案：**

> 我会按照数据价值和读写频次进行分层设计：
> 1. **状态和轻量数据**：用 **MMKV** 代替 `UserDefaults`。它底层基于 `mmap`（内存映射），文件直接映射到用户空间，实现了**零拷贝（Zero-Copy）**，读写速度比传统 I/O 阻塞的 Plist 快几个数量级，且并发安全。
> 2. **复杂关系型数据**：使用 SQLite，做好数据库的版本迁移，并用读写锁防并发冲突。
> 
> **一致性设计**：遵循“单向数据更新源”原则。首屏秒开时读取本地缓存展示，同时后台发起网络请求，数据回来后更新数据库并驱动 UI 局部刷新。关键路径（如交易、购物车）状态以服务端确认为准，本地做乐观锁或事务回滚。

---

## 16：包体积和编译速度你怎么优化？

**口语答案：**

> **包体积优化**：先分析 Mach-O 文件和资源占比。压缩图片（改用 WebP 格式），删除无用图片和重复资源；利用脚本扫描 Mach-O 里的无用类和无用 Selector 并清理。
> 
> **编译速度优化**：
> - **控制依赖**：OC 项目中严格控制公共头文件（如 `.h`）的互相 import，多使用 `@class` 进行前向声明，防止修改一个头文件导致大面积重新编译。
> - **工程治理**：组件化后，将稳定的基础库和业务组件编译为二进制库，CI/CD 阶段使用二进制依赖，避免每次全量源码编译。

---

## 17：__weak 修饰符的底层实现原理是什么？

**口语答案：**

> weak 的本质是：**当指向的对象释放时，指针会自动置为 nil。**
> 
> 底层是由 Runtime 维护的全局弱引用表（`SideTables`）实现的，这是一个以**对象内存地址为 Key**，**弱引用指针地址数组为 Value** 的哈希表。
> 
> **核心步骤分三步：**
> 1. **初始化**：你声明 `__weak` 时，Runtime 会把指针的地址注册到对应对象的弱引用数组中。
> 2. **析构触发**：当对象释放执行 `dealloc` 时，Runtime 会根据对象地址查表找到它的弱引用记录。
> 3. **置空指针**：遍历该记录下的所有弱引用指针全部赋值为 `nil`，最后从表中清除该记录。
> 
> **性能注意**：`SideTables` 读写需要分段加锁。极高频的循环体或高频调用的方法中，要避免频繁访问 weak 指针，防止哈希查找和锁竞争开销。

---

## 18：你怎么提升 iOS 业务代码的可测试性？

**口语答案：**

> 可测试性的前提是**解耦**和**依赖隔离**。
> 
> 1. **接口协议化与依赖注入**：网络、数据库、时间、定位等外部依赖不要写死在业务类中，而是定义为 Protocol。在类初始化时通过注入方式传入。测试时传入 Mock 实例，实现单元测试的完全可控。
> 2. **业务逻辑抽离**：避免 ViewController 承担过多逻辑，将纯数据计算、状态转移逻辑剥离到 ViewModel 或 Model 中，脱离 UIKit 生命周期，方便编写轻量、快速运行的 Unit Test。
> 3. **聚焦核心**：优先测试高风险的复杂业务规则（如缓存淘汰、账单计算、登录状态机），而不是把精力耗费在变动频繁的 UI 布局测试上。

---

## 19：RunLoop 的本质是什么？你在哪些场景真正需要关注它？

**口语答案：**

> RunLoop 本质是一个事件循环。它的工作是“有事件时唤醒处理，无事件时进入休眠状态以节省 CPU 资源”，管理着输入源、Timer 以及 Observer。
> 
> **实际需要关注的场景有：**
> 1. **卡顿监控**：通过向 RunLoop 注册 Observer 监听状态变化。如果从进入事件处理到即将休眠的停留时间过长，说明主线程卡顿，此时抓取堆栈。
> 2. **列表滑动时 Timer 失效**：滑动列表时 RunLoop 模式会切到 `UITrackingRunLoopMode`，导致加在 `DefaultMode` 的 Timer 暂停。解决方法是将 Timer 加入 `NSRunLoopCommonModes`。
> 3. **自动释放池释放时机**：RunLoop 的一轮循环结束即将休眠前，会自动 drain 当前的自动释放池。

---

# P2：加分题

## 20：Method Swizzling 的原理和风险是什么？

**口语答案：**

> Method Swizzling 是运行时交换方法的实现。本质是在类的方法列表中，将 selector 对应的 IMP（函数指针）相互交换。
> 
> **工程风险：**
> 1. **执行顺序不可控**：如果多个模块同时对同一个方法进行 Swizzle，一旦顺序乱了，极易引发死循环或功能失效。
> 2. **父类方法继承风险**：如果子类本身没有重写父类方法，直接进行交换会误将父类的方法实现修改掉，影响该父类的其他所有子类。
> 
> **防范规范**：只在 `+load` 中使用，且必须包裹在 `dispatch_once` 中确保只交换一次；对于未实现的函数，先尝试用 `class_addMethod` 动态添加，失败后再交换。能用协议、组合解决的，不优先使用 Swizzling。

---

## 21：Category 的加载机制是什么？+load 和 +initialize 有什么区别？

**口语答案：**

> **Category 的加载**：编译期每个分类生成单独的结构，在 Runtime 启动时，分类的方法、属性、协议会被动态合并到原类的方法列表中，并插在原类方法的最前面（因此分类同名方法会覆盖原类）。由于对象内存布局在编译期已确定，分类不能真正添加成员变量，只能用关联对象模拟。
> 
> **+load 和 +initialize 的区别：**
> - **+load**：在 main 函数执行前被调用。当类或分类被加载入系统时调用一次。它的调用不经过消息发送，直接调用函数指针。所有类和分类的 +load 都会执行。
> - **+initialize**：在类首次收到第一条消息时懒加载调用。它是通过正常的消息发送机制调用的，如果子类未实现，会继承并触发父类的 `+initialize`（可能导致父类实现被执行多次）。
> 
> *答题提示：在 +initialize 中必须加判断 `if (self == [CurrentClass class])` 防范父类逻辑被多次执行。*

---

## 22：AutoreleasePool 在什么场景下需要你主动关注？

**口语答案：**

> 日常开发中，主线程 RunLoop 每轮循环结束都会自动 drain 池子，所以不需要手动管理。但在**短时间内大量创建临时对象**的场景下，必须手动包裹 `@autoreleasepool { ... }` 闭包：
> 
> **核心场景：**
> 1. **高频循环体**：比如一个循环执行几万次，循环内不断解析 JSON、读取本地图片（`UIImage` 底层是 CGImage 属于 OC 对象）或调用 OC 的临时方法。若不手动包裹，这些内存要在 RunLoop 闲置时才释放，这会造成内存峰值瞬间飙升，极易触发 Jetsam OOM 崩溃。
> 2. **常驻后台线程**：后台线程如果没有开启 RunLoop，其自动释放的对象难以主动释放，需用自动释放池显式包裹长生命周期任务。
> 
> *答题提示：重点是理解"对象最终会释放"和"释放时机太晚导致峰值过高"是两回事。很多 OOM 问题不是泄漏，而是峰值控制失败。*

---

## 23：如果让你接手一个历史包袱很重的 iOS 模块，你会怎么治理？

**口语答案：**

> 治理历史包袱模块，大改容易翻车，我遵循“小步快跑、数据说话”的原则：
> 
> **第一步：现状量化**。通过线上监控指标（崩溃率、卡死率、冷启动耗时、大对象占比）摸清现状，找出最急需解决的痛点。
> **第二步：边界隔离**。在 ViewController 与业务逻辑间定义接口协议。将网络、数据缓存、页面跳转彻底抽出，使得新需求可以使用新设计的模块，旧模块则逐步重构。
> **第三步：风险控制**。坚决不搞一次性全量上线的重构。重构代码必须做灰度发布，配置好动态开关（AB测试），一旦线上有异常指标能立刻回滚。

---

## 24：什么是 Tagged Pointer？它对 iOS 开发有什么影响？

**口语答案：**

> Tagged Pointer 是一种优化 64 位系统下小对象（如 `NSNumber`、`NSDate`、短 `NSString`）内存和访问性能的机制。
> 
> **原理**：
> 普通对象需要在堆区分配内存、维护引用计数，而小对象的值本身很小（8字节内）。Tagged Pointer 直接把**对象的值和类型标记编码进了指针地址本身**，不走堆内存分配，不参与 ARC 计数。
> 
> **开发影响**：
> 1. **极大提升性能**：省去了堆内存申请和释放的时间，省去了消息查找开销。
> 2. **野指针风险低**：因为不分配在堆区，不走正常的 `dealloc`，不存在普通对象的野指针问题。但需要注意，在多线程高频给一个 strong 的 Tagged Pointer 属性赋值时，ARC 插入的代码仍可能因为非原子操作引发多线程竞争 Crash。

---

## 25：如果让你设计一个 iOS 端的热修复或动态化方案，你会考虑哪些因素？

**口语答案：**

> 我会着重权衡三个维度：
> 
> 1. **苹果审核红线**：这是首要考虑。Runtime 级别的热修方案（如 JSPatch）由于能远程动态改变执行代码，面临极大的下架审核风险。因此方案应该仅限用于核心线上 Bug 的临时自救，且需要做签名验签，防范中间人代码篡改。
> 2. **替代策略**：能通过“动态路由降级”、“配置中心开关下发”解决的，坚决不进行代码级热修复。
> 3. **灰度与容灾**：热修脚本下发必须分批灰度，且 App 启动时要设计“热修容灾”，如果连续启动 Crash，直接在下一次启动时强行清空本地热修缓存。

---

# 架构篇：架构设计与工程治理

## 26：你怎么设计一个可维护的 MVVM 或类似分层架构？

**口语答案：**

> 在 OC 项目里，MVVM 的核心是**让 ViewController 瘦身**，ViewController 仅负责生命周期、UIKit 布局以及数据绑定。
> 
> **分层逻辑：**
> - **ViewModel**：负责把 Model 转换成 UI 直接展示的数据（比如时间格式化），处理网络请求和业务逻辑。它绝对不能强持有 View，以防循环引用。
> - **Model / Service**：定义数据实体。
> 
> **数据绑定**：不需要过度引入复杂的 RAC。可以使用轻量的 Block 闭包回调、代理或者 KVO，ViewModel 修改属性后，主动回调 View 进行更新，保持单向的数据流动，极大方便了对逻辑代码编写单元测试。

---

## 27：组件化和路由方案怎么设计，才能避免后期失控？

**口语答案：**

> 组件化的核心是**解除业务模块间的直接依赖**，而不是盲目分库。
> 
> **我的设计思路：**
> 1. **三层划分**：基础功能库（网络、缓存） -> 基础业务组件（分享、支付） -> 业务线模块（购物车、详情页）。模块间严格单向依赖，禁止同层逆向依赖。
> 2. **跨模块调用双轨制**：
>    - **页面跳转**：使用轻量 **URL 路由**，便于承接外部 H5 唤起或 Scheme 降级。
>    - **服务调用**：使用 **Protocol-Service 注册方案**。各组件在组件内实现对应协议，外部通过 Service 容器获取明确类型，避免路由参数全部字典化、字符串化导致的维护灾难。

---

# 面试表达建议

1. **套用框架**：一句话结论 → 核心原理 → 我怎么做 → 风险/取舍。
2. 每题控制在 60 到 90 秒，不要一开始就陷入源码细节。
3. 少背 API 名字，多讲取舍、风险、验证方式。
4. 如果没有真实项目经验，不要硬编，可以说"我理解的处理方式是"，但不要伪造成线上案例。
5. 高级面试更看重定位问题和治理复杂度的能力，不只是知道某个方法怎么调用。
