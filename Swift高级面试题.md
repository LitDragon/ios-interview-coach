# Swift 高级面试题排序版

> 目标：按高级 iOS 面试中的重要性和区分度排序。答案采用面对面口语表达，每题建议控制在 60 到 90 秒。
>
> 筛选原则：删除单纯语法记忆、代码默写、偏炫技的底层细节；把必要基础合并到更高级的问题里回答。

---

## 题目分级

| 优先级 | 定位 | 题量 |
| --- | --- | --- |
| P0 | 必须讲清楚，决定 Swift 高级开发基本面 | 10 题 |
| P1 | 高频进阶题，体现工程深度和项目经验 | 9 题 |
| P2 | 加分题，体现底层理解和架构判断 | 5 题 |

## 已删除或合并的低价值题

- “Optional 枚举定义”“associated type 代码示例”不再单独背代码，合并到类型系统和泛型题里。
- “Metadata 具体字段布局”“对象前多少字节”弱化为底层理解题，不作为主线题。
- “手写响应式绑定”“手写任务队列”“手写缓存代码”改成设计取舍题，不要求现场读代码。
- “SwiftUI 属性包装器清单”合并为状态管理和数据流问题。
- “struct 一定在栈上、class 一定低性能”这类容易失真的表述已修正。

---

# P0：高级 Swift 必会题

## 01：Swift 的值语义和引用语义怎么理解？struct 和 class 应该怎么选？

**口语答案：**

> 我会先说结论：struct 代表值语义，class 代表引用语义。值语义强调每个变量看起来都有独立的值，修改一个不会影响另一个；引用语义强调多个变量可以指向同一个对象，修改会被共享观察到。
>
> Swift 推荐优先用 struct，不是因为 struct 一定在栈上，也不是因为 class 一定慢，而是因为值语义更容易推理，线程共享状态更少，也不会产生对象之间的循环引用。但如果对象需要身份、继承、共享可变状态、生命周期管理，class 仍然是合理选择。
>
> 项目里我一般让 Model、配置、状态快照优先用 struct；需要被多处共享、需要观察生命周期、需要和 UIKit 或 Objective-C 体系交互的对象用 class。高级面试里重点不是“选哪个”，而是能说明所有权、可变性和共享状态的边界。

---

## 02：Copy-on-Write 是什么？它解决了什么问题？

**口语答案：**

> Copy-on-Write 叫写时复制，核心是让值类型在读和传递时尽量共享底层存储，只有发生修改并且底层存储不唯一时，才真正复制一份。Swift 的 Array、Dictionary、String 都大量使用这个机制。
>
> 它解决的是值语义和性能之间的矛盾。值语义要求修改互不影响，但如果每次传参和赋值都深拷贝大数组，性能会很差。COW 让大多数读操作保持低成本，只有写入时才付出复制代价。
>
> 我会补一句风险：COW 不等于线程安全。如果多个线程同时读写同一个变量或同一份共享状态，仍然可能有数据竞争。自定义 COW 类型时，也要明确唯一引用判断和写入时机，否则很容易破坏值语义。

---

## 03：Swift 的 Optional 体现了什么类型安全思想？

**口语答案：**

> Optional 本质上表达的是“这个值可能不存在”，它把空值从运行时约定提升到了类型系统里。调用方必须显式处理有值和无值两种情况，编译器会帮你拦住很多漏判空的问题。
>
> 它的价值不是背出 Optional 是泛型枚举，而是理解 Swift 不希望把空值当成普通值随处传播。通过可选绑定、guard、空合并和可选链，代码会更明确地表达失败路径和兜底逻辑。
>
> 但 Optional 也不是万能安全。强制解包仍然会崩，隐式解包如果生命周期假设错了也会出问题。高级项目里我会尽量让 Optional 只出现在边界层，比如接口返回、配置缺失、弱引用，而不是让核心业务里到处都是不确定状态。

---

## 04：Swift 的 ARC 和闭包捕获有哪些高频坑？

**口语答案：**

> Swift 里 ARC 主要管理 class 实例，值类型本身不靠引用计数。循环引用最常见的来源是对象持有闭包，闭包又强持有对象，比如 ViewModel 持有回调、控制器持有任务、Timer 或通知回调里捕获 self。
>
> 我处理时不会无脑写 weak self，而是先判断闭包生命周期。如果是非逃逸闭包，通常不需要 weak；如果闭包会被对象长期持有，就要用 weak 打断闭环。unowned 只适合生命周期严格保证被捕获对象更长的场景，否则访问已释放对象会直接崩溃。
>
> 面试时我会强调：内存问题不是语法问题，而是所有权设计问题。谁持有任务、任务完成后是否释放、取消时闭包是否还在、回调回来时对象是否还有效，这些才是 Swift 项目里真正容易出问题的地方。

---

## 05：GCD 和 Swift Concurrency 怎么取舍？

**口语答案：**

> GCD 是基于队列的底层调度工具，适合简单异步、回主线程、串行队列保护资源，以及和老 Objective-C 代码协作。Swift Concurrency 是语言级并发模型，用 async/await、Task、TaskGroup、Actor 表达异步流程、取消、错误传播和隔离。
>
> 新 Swift 业务里，我会优先用 async/await 写单次异步流程，因为代码更线性，也更容易传递错误和取消。遇到老模块、底层 C API、需要指定队列、需要 barrier 或已有 GCD 体系时，GCD 仍然有价值。
>
> 真正要避免的是混用失控。比如在 Task 里塞大量同步阻塞 IO，可能占住协作式线程池；或者在 GCD 回调里乱改 MainActor 状态。我的原则是并发边界要统一，UI 状态交给 MainActor，阻塞任务要隔离出去。

---

## 06：Actor 解决了什么问题？它有哪些容易忽略的风险？

**口语答案：**

> Actor 解决的是共享可变状态的数据竞争问题。它把内部可变状态隔离起来，外部访问通常需要 await，同一时间只允许一个任务进入 Actor 的隔离上下文，从语言层面降低了手写锁的风险。
>
> 但 Actor 不是“自动无 bug 的锁”。第一个风险是重入：Actor 方法里一旦遇到 await，当前任务会挂起，其他任务可能进入并修改状态，恢复后原来读到的状态可能已经过期。第二个风险是把耗时同步任务放进 Actor，会拖慢所有等待这个 Actor 的任务。
>
> 所以我会让 Actor 管理状态一致性，但避免在隔离方法里做长时间阻塞。await 之后重新校验关键状态，必要时把纯计算、IO、网络请求和状态提交拆开，这样才能真正用好 Actor。

---

## 07：MainActor 的作用是什么？Swift 并发下怎么保证 UI 更新安全？

**口语答案：**

> MainActor 是绑定主线程语义的全局 Actor。它的价值是把“UI 必须在主线程更新”从人为约定变成编译器可以检查的隔离规则。
>
> 在 SwiftUI 或现代 UIKit 项目里，我倾向于把负责页面状态的 ViewModel 标记在 MainActor 上，让状态变更和 UI 观察都在主线程语义内发生。后台请求、解析、缓存可以放到非主线程执行，最后只把需要展示的结果提交回 MainActor。
>
> 需要注意的是，MainActor 不等于所有代码永远同步跑在主线程上，它表达的是隔离域。异步函数中跨 Actor 调用会有挂起点，顺序和取消都要考虑。面试里如果只说“更新 UI 要回主线程”，是不够高级的。

---

## 08：Swift Concurrency 里的取消、错误传播和结构化并发怎么理解？

**口语答案：**

> 结构化并发的核心是任务有层级关系。父任务创建子任务后，子任务的生命周期、取消和错误可以随着作用域一起管理，这比以前散落的回调和 GCD 任务更容易推理。
>
> 取消在 Swift 里是协作式的，不是系统强行杀掉任务。任务要在合适位置检查取消状态，遇到取消后及时停止后续工作、释放资源、避免再更新 UI。错误传播也是类似，子任务失败时要明确是让父任务失败，还是收集部分结果继续走降级逻辑。
>
> 项目里我会把页面请求、并发加载、批量任务放进清晰的任务作用域里，页面消失时取消对应任务。这样可以减少无效请求、错乱回调和对象被任务延长生命周期的问题。

---

## 09：SwiftUI 的声明式 UI 和状态驱动怎么落地？

**口语答案：**

> SwiftUI 的核心可以理解为 UI 是状态的结果。开发者不再手动命令某个控件逐步变化，而是维护状态，状态变化后由框架重新计算视图描述，再做差异更新。
>
> 真正落地时，关键是状态归属要清楚。页面内部临时状态放在 View 自己管理；跨页面或复杂业务状态放到 ViewModel 或独立 Store；父子视图之间只传必要状态和事件，不要让每个子视图都随便改全局数据。
>
> SwiftUI 最常见的问题不是不会写属性包装器，而是状态散乱、重复数据源、异步任务和视图生命周期耦合不清。高级实践会偏向单向数据流：View 发送用户意图，状态层处理副作用并产出新状态，View 只负责渲染。

---

## 10：SwiftUI 或 Swift 项目里的 MVVM 应该怎么设计才不臃肿？

**口语答案：**

> MVVM 不是把控制器里的代码搬进 ViewModel。好的 ViewModel 应该负责页面状态、用户意图处理、展示数据转换和副作用编排；网络、数据库、定位、支付这些能力应该下沉到 Service 或 Repository。
>
> 在 SwiftUI 里，我会让 View 尽量保持轻，主要做布局和事件转发。ViewModel 如果管理 UI 状态，通常要考虑 MainActor；如果涉及复杂业务流程，就拆成 UseCase 或独立服务，避免 ViewModel 变成新的上帝对象。
>
> 衡量标准是可测试性和可维护性。一个页面出了问题，应该能快速定位是视图状态、业务规则、数据源还是副作用失败，而不是所有逻辑都混在一个巨大 ViewModel 里。

---

# P1：高频 Swift 进阶题

## 11：面向协议编程 POP 的价值是什么？有什么滥用风险？

**口语答案：**

> POP 的价值是用协议表达能力，用组合替代一部分继承，让 struct、enum、class 都能参与抽象。协议扩展还可以提供默认实现，减少重复代码。
>
> 它适合定义稳定边界，比如网络请求能力、缓存接口、路由服务、格式化策略。相比继承，协议组合更轻，也更利于测试替换。
>
> 但 POP 也容易滥用。协议过多会让调用链变碎，associated type 和泛型约束过度会让 API 难用，默认实现太重会隐藏行为。我的原则是：协议应该来自真实变化点，而不是为了“面向协议”而抽象。

---

## 12：泛型、associated type、any、some 之间是什么关系？

**口语答案：**

> 泛型是在编译期把类型参数化，让一套逻辑适配多种类型，同时尽量保持类型安全和性能。associated type 是协议里的占位类型，用来表达“实现这个协议的具体类型自己决定某个关联类型”。
>
> some 表示不透明类型，调用方不知道具体类型名，但编译器知道它始终是同一种具体类型，所以性能和类型检查更好。any 表示存在类型，可以装不同的具体实现，但会带来动态派发和一定包装成本。
>
> 实战里我会优先用泛型和 some 保持静态类型信息，只有需要异构集合、运行时替换实现、模块边界隐藏具体类型时才用 any。面试重点是能讲清楚灵活性和性能、可读性之间的取舍。

---

## 13：Combine 和 async/await 应该怎么选择？

**口语答案：**

> async/await 更适合一次性的异步流程，比如请求接口、读文件、拉配置、提交表单。它像同步代码一样表达顺序，错误处理和取消也更清楚。
>
> Combine 更适合持续变化的数据流，比如搜索输入防抖、多个状态组合、实时消息、表单校验、视图状态绑定。它的优势是处理随时间变化的事件流，但调试和生命周期管理成本更高。
>
> 项目里我不会为了统一而强行只用一种。单次请求用 async/await，持续状态流用 Combine 或 SwiftUI 的观察机制。关键是边界清楚，不要让 Publisher、Task、回调互相嵌套到看不懂。

---

## 14：Swift 方法派发机制对性能和动态能力有什么影响？

**口语答案：**

> Swift 方法调用大致可以分为静态派发、函数表派发和 Objective-C 消息派发。struct、enum、final、private 等更容易走静态派发，编译器可以内联优化；普通 class 方法通常通过函数表支持重写；标记为 Objective-C 动态的方法会走更动态的消息机制。
>
> 这影响两个方面：性能和能力。静态派发性能更好，但动态替换能力弱；消息派发更灵活，能支持 KVO、Runtime、Swizzling，但也有查找和动态行为成本。
>
> 实战里我不会为了微优化到处加 final，但在确定不会继承的类型上使用 final 能表达设计意图，也给编译器优化空间。需要暴露给 Objective-C 或 Runtime 的能力时，再选择动态派发。

---

## 15：Swift 的内存泄漏怎么检测和治理？

**口语答案：**

> Swift 泄漏高发点主要是闭包捕获 self、Timer、Notification、异步 Task、Combine 订阅、delegate 或数据源所有权设计不清。Swift 写得多不代表不会泄漏，因为 class、闭包和框架对象仍然是引用语义。
>
> 我会先在页面退出后确认 deinit 是否发生，再用 Xcode Memory Graph 看引用链。复杂场景配合 Instruments 的 Leaks 和 Allocations，看对象增长和持有路径。对于高风险页面，可以在测试里加释放断言。
>
> 治理上重点是明确生命周期。订阅要有取消点，Task 要随页面释放或消失取消，闭包捕获要按生命周期选择 weak 或 unowned，缓存要有淘汰策略。只靠扫描 weak self 是治标不治本。

---

## 16：依赖注入在 Swift 项目里有什么价值？怎么落地？

**口语答案：**

> 依赖注入的价值是把对象需要的外部能力从内部创建改成外部传入。这样业务对象不再硬编码网络层、数据库、定位、时间等依赖，测试时可以替换成 mock，模块之间也更容易解耦。
>
> Swift 里最稳的方式是构造注入，因为依赖在初始化时就明确，编译器能保证必需依赖存在。对于跨页面、跨模块的共享服务，可以用轻量容器或 Environment 风格注入，但要控制全局状态带来的隐式依赖。
>
> 我会避免把 DI 容器变成万能单例。业务核心依赖尽量显式传入，只有非常通用的环境能力才通过容器解决。高级项目更看重依赖关系是否可读、可替换、可测试。

---

## 17：Swift 和 Objective-C 混编时要注意什么？

**口语答案：**

> 混编首先要注意类型边界。Swift 的 Optional、泛型、enum、struct 并不能都无损暴露给 Objective-C；Objective-C 的 nullable 标注不准确，也会影响 Swift 侧的类型安全。
>
> 其次是动态能力。Objective-C 依赖 Runtime，Swift 默认更静态；如果 Swift 方法要支持 KVO、selector、Swizzling 或给 Objective-C 调用，需要明确暴露动态入口，但这也会牺牲一部分静态优化。
>
> 还有内存和错误模型差异。Core Foundation 桥接、NSError 与 Swift Error、集合可变性、线程回调位置都要处理清楚。大型项目里我会让混编边界集中在 adapter 层，避免 Swift 业务代码到处被 Objective-C 约束污染。

---

## 18：Swift 代码的编译慢、包体积变大，你会怎么排查？

**口语答案：**

> 编译慢我会先看是否有大文件、复杂泛型、过度类型推断、巨大的 SwiftUI body、跨模块依赖过重。这些都会让编译器做更多推断和特化工作。
>
> 处理方式包括拆小文件和表达式、给复杂闭包或泛型补明确类型、减少公共接口暴露、优化模块依赖、清理无用代码和资源。SwiftUI 页面尤其要把复杂 body 拆成小视图，减少类型推断压力。
>
> 包体积则要看资源、三方库、符号、泛型特化、架构切片和重复依赖。不能只凭感觉删代码，要先量化来源，再做有验证的清理。

---

## 19：Swift 项目如何提升可测试性？

**口语答案：**

> 可测试性来自边界清楚。网络、缓存、时间、定位、权限、支付这些外部依赖要通过协议或注入隔离，业务逻辑不要直接绑死在 View 或系统 API 上。
>
> 我会优先测试纯逻辑和状态变化，比如 ViewModel 的状态流、请求成功失败后的展示状态、缓存策略、并发取消、权限分支。Swift 的值类型和不可变状态很适合做这类测试，因为输入输出更明确。
>
> UI 测试适合覆盖关键链路，不适合验证所有细节。高级工程师应该能把复杂业务拆到可测试的层里，而不是把所有风险都留给手工点页面。

---

# P2：加分 Swift 题

## 20：Swift Metadata、Witness Table、V-Table 这些底层概念有什么实际意义？

**口语答案：**

> 这些概念不需要在业务里天天写，但能帮助理解 Swift 的泛型、协议和动态派发为什么有性能差异。Metadata 描述类型信息，V-Table 支持 class 的动态重写，Witness Table 让协议调用能找到具体实现。
>
> 当泛型能被编译器特化时，性能通常更接近直接调用；当使用协议存在类型时，可能需要通过表做动态派发和包装。知道这些，有助于解释为什么 some、泛型和 any 的取舍不只是语法偏好。
>
> 面试里我不会背字段布局，而会讲它们和真实工程的联系：性能分析、二进制体积、协议抽象成本、动态能力边界。能把底层概念落到工程决策上，才有价值。

---

## 21：autoreleasepool 在 Swift 中什么场景需要关注？

**口语答案：**

> 纯 Swift 值类型代码里通常不用主动关心 autoreleasepool。但 iOS 项目大量使用 Foundation、UIKit、Core Graphics，这些对象和 Objective-C 运行时仍然有关联，临时对象可能进入自动释放池。
>
> 需要关注的场景是批量循环处理图片、Data、字符串桥接、JSON、文件导入导出，或者后台线程长时间创建 Objective-C 对象。如果不缩短释放周期，可能不是泄漏，却会造成内存峰值过高甚至 OOM。
>
> 我会在批处理里分批建立释放边界，让临时对象尽快释放。重点是区分“对象最终会释放”和“释放太晚导致峰值不可接受”。

---

## 22：如果要设计一个高性能线程安全缓存，你会怎么讲方案？

**口语答案：**

> 我会先明确缓存目标：是内存缓存还是磁盘缓存，是图片还是业务数据，是追求命中率、低延迟还是一致性。不同目标决定淘汰策略和并发模型。
>
> 内存缓存通常需要容量上限、成本计算、过期时间、内存警告清理和命中率统计。线程安全上可以用锁、并发队列加 barrier，或者 Actor；如果读多写少，读写锁或 barrier 思路更合适；如果逻辑状态复杂，Actor 可读性更好。
>
> 我不会一上来就说 LRU。LRU 适合很多场景，但图片缓存还要考虑解码后成本，业务缓存要考虑数据版本和用户隔离。高级答案应该体现约束、指标和取舍。

---

## 23：如果要设计一个可取消、有依赖的异步任务系统，你会怎么选型？

**口语答案：**

> 如果项目已经有 OperationQueue，或者任务天然有依赖、优先级、最大并发数、暂停恢复，OperationQueue 仍然是成熟选择。它的模型很适合下载队列、批处理、导入导出这类任务。
>
> 如果是现代 Swift 代码，TaskGroup 和结构化并发更适合表达一组有作用域的异步任务。它的取消和错误传播更自然，但复杂依赖图、持久化队列和跨页面调度仍然需要额外设计。
>
> 我的选型标准是任务生命周期归属。如果任务跟页面强相关，用结构化并发更清楚；如果任务需要跨页面、可暂停、可恢复、可排队，独立任务系统或 OperationQueue 更稳。

---

## 24：Property Wrapper 的价值是什么？什么时候不该用？

**口语答案：**

> Property Wrapper 的价值是把属性访问时的重复逻辑封装起来，比如状态存储、UserDefaults 映射、依赖注入、线程隔离、数据校验。它能让使用方看起来像普通属性，但背后带有统一行为。
>
> 不该用的场景是业务语义很复杂、隐藏副作用太多、调试成本高，或者团队成员看到属性声明无法判断真实行为。比如一个属性读写时偷偷发网络请求或改全局状态，就会让代码非常难维护。
>
> 我会把 Property Wrapper 用在稳定、简单、可预期的横切逻辑上，并保持命名清晰。它是封装工具，不是让业务逻辑消失的魔法。

---

# 面试表达建议

1. 先讲结论，再讲机制，最后讲项目取舍。
2. Swift 题不要只背语法，要主动补充所有权、并发安全、可测试性和可维护性。
3. 每题控制在 60 到 90 秒，避免现场展开源码级细节。
4. 遇到底层题，少背字段，多讲它对性能、动态能力和工程决策的影响。
5. 没有真实项目经验时，不要伪造案例，可以说“我会按这个思路处理”。
