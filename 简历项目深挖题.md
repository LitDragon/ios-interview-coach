# 高级 iOS 简历项目深挖题

> 目标：把真实工作记录转换成可以稳定讲 30 秒、90 秒，并能承接连续追问的项目答案。
>
> 统一结构：**背景与约束 → 个人职责 → 证据链 → 根因 → 方案与取舍 → 验证 → 剩余风险**。
>
> 以下三个 P0 案例已核对当前 V720 代码和本人 Git 提交。面试前仍需确认团队规模、业务影响及可公开范围。

---

# P0 案例一：直播切回放的异步状态所有权

## 30 秒版本

我处理过一个直播切 SD 卡回放的状态污染问题。旧流程在播放器真正出首帧前，就已经暂停直播音频、录音和定时器并切换媒体模式；如果准备失败、超时或用户离页，晚到回调仍可能继续跳转或重新连接，导致直播无法恢复。我的处理是把“首个视频帧解码”定义为唯一成功终态，错误、超时和取消统一走回滚，同时用取消标记、请求代次和页面可见性拒绝晚到事件，保证每个终态只清理一次。

## 90 秒版本

背景是非 AP 直播进入 SD 卡回放。准备阶段会先调用 `prepareLiveSessionForPlaybackTransition`，暂停音频、录音和部分 timer，并把直播媒体模式从 AV 切到 N。但旧逻辑在拿到日期或文件列表后就认为可以展示回放页，实际上 IJK 播放器可能还没有解码出第一帧；一旦 405/407 失败、播放器首帧前报错、10 秒超时或页面退出，直播状态就可能停留在半切换状态。

我先按 App、ViewModel、Media 三层还原状态边：

1. `QGCheckRecordViewModel` 负责回放准备请求和取消归属；
2. `NaxclowMediaComponent` 负责播放器以及首帧、错误事件；
3. `QGLiveSessionCoordinator` 负责直播音频、录音、timer 和媒体模式恢复。

实现上，播放器 reset 后设置 `awaitingFirstVideoFrame`；只有首帧事件 `method=20004` 才允许完成准备并展示页面。`method=20001`、405/407 非成功状态、无可播放文件和超时都进入失败路径。取消时设置门禁，释放播放器并断开回放连接，再调用 `cancelPlaybackTransitionAndRestoreLiveSession` 恢复原来的 AV 状态和此前确实存在的 timer。后续又补了请求代次、应用前台和页面可见性判断，避免后台首帧、旧 provider completion 或重复 `viewWillAppear` 再次启动链路。

取舍上没有重写成大状态机，而是沿用原有层次，只增加首帧终态、取消门禁和统一 cleanup，降低影响范围。

## 验证证据

- `NaxclowMediaLogicTests` 覆盖首帧只上报一次、首帧前错误、释放后不再上报终态。
- 后续提交分别覆盖首次展示重复初始化、后台首帧、迟到直播配置和对讲误恢复。
- 通用 iOS 构建通过；真机受控超时、后台首帧和设备注入仍应如实列为剩余验证。

## 高频追问

### 为什么不能在拿到文件列表后就跳转？

文件列表只说明业务数据存在，不代表播放器已经建立可展示的媒体状态。真正决定用户能否看到回放的是首帧解码，因此成功终态必须放在 Media 层的首帧事件。

### 为什么取消时既要 releasePlayer 又要 disconnectPlayback？

`releasePlayer` 终止本地播放器事件，`disconnectPlayback` 释放设备或 SDK 侧回放连接。只做其中一个，都可能留下迟到媒体事件或连接所有权。

### 为什么不用一个全局 Boolean？

Boolean 只能表达“忙或不忙”，无法区分旧请求、新请求、设备和页面生命周期。晚到回调需要用取消状态、attempt/generation 和当前页面条件共同判断。

### 如果重新设计会怎么做？

如果后续继续增加入口，会把准备过程收敛为明确的 `idle/preparing/awaitingFirstFrame/presented/cancelled/failed` 状态；当前修复选择局部门禁，是为了控制存量工程改动风险。

## 代码与提交证据

- `41f9d438`：首帧 readiness、首帧前错误及逻辑测试。
- `05a2bcc1`：首次展示重复连接和播放器 reset。
- `97b09dfc`：后台停止后的迟到直播配置。
- `68028576`：后台首帧导致直播无法恢复。
- `044edb78`：失败回滚误开启对讲录音。

---

# P0 案例二：畸形设备协议载荷导致越界崩溃

## 30 秒版本

我定位过一个 AP 模式的协议崩溃：数据包头是完整的，但 G-sensor 命令声明的 payload 只有 1–5 字节，消费端却固定读取三个 `int16_t`，最终触发 `getBytes:range:` 越界。修复没有改动通用分帧器，而是在最了解字段需求的命令消费者处，用 `3 * sizeof(int16_t)` 做长度门禁，并补了 1–5 字节不崩、6 字节保持正常路径的边界测试。

## 90 秒版本

问题最初看起来像网络短包，但我沿着 `NaxclowApChannel → NaxclowApProtocolParser → NaxclowClientAp` 追踪后确认，20 字节协议头已经完整读取；真正的异常是 header 合法地声明了一个过短的 payload。解析器按声明长度把数据交给 `P2P_UDP_CMD_G_SENSOR`，消费者随后无条件读取 X、Y、Z 三个 `int16_t`，所以 1–5 字节都会在 Foundation range 检查处崩溃。

修复点放在 `NaxclowClientAp` 的 G-sensor 分支，因为只有这里知道实际字段合同。长度条件写成 `data.length < 3 * sizeof(int16_t)`，而不是魔法数字 6；不足时记录日志并丢弃当前命令，避免影响连接和其他协议消息。这样不改变通用 header framing，也不影响 6 字节及以上的正常包。

测试保留了最关键的边界：循环注入 1–5 字节 payload，断言不抛异常；再注入 6 字节，断言仍走正常消费路径。这个案例体现的是先区分“header 不完整”和“字段 payload 不完整”，再在拥有字段语义的最窄位置止血。

## 验证证据

- Foundation 复现确认 1–5 字节为崩溃边界，6 字节不越界。
- `NaxclowNetChannelTests` 添加 1–5 和 6 字节回归用例。
- SDK build-for-testing 通过；Host App 或真实 AP/TCP 注入仍需作为运行时验证。

## 高频追问

### 为什么不在通用 parser 里统一丢弃短 payload？

parser 只知道 header 声明了多少数据，不知道每个命令实际需要哪些字段。把 G-sensor 的 6 字节要求放进通用层会耦合业务协议，也可能错误拒绝其他允许短 payload 的命令。

### 为什么不只判断 `payloadLen`？

最终执行 range 读取的是 `NSData`，所以消费前应以实际 `data.length` 为安全边界；header 值可以用于日志和协议一致性检查，但不能替代真实缓冲区长度。

### 丢弃数据后为什么不主动断开？

单个传感器包不完整不应破坏直播连接。记录并丢弃是最小影响策略；如果持续出现，再由监控或协议层决定是否升级处理。

## 代码与提交证据

- `e874eb13`：`NaxclowClientAp` 长度门禁和 `NaxclowNetChannelTests` 边界用例。

---

# P0 案例三：NanoRTSP 静态 XCFramework 集成

## 30 秒版本

我负责过将 NanoRTSP 从旧 Framework 形态替换为静态 XCFramework。难点不只是复制产物，而是同时处理 device/simulator 架构、公开头文件和 module map、源码 import，以及 Xcode 的 link/embed 语义。我保留 Frameworks 链接、移除静态库不需要的 Embed 项，最终确认 device arm64 和 simulator arm64/x86_64 切片、模块导入及实际链接链路。

## 90 秒版本

背景是媒体层需要接入一套纯 C 的 NanoRTSP 实现，并同时支持真机和模拟器。旧产物以 Framework 目录存在，但新的目标是静态 XCFramework；如果只替换文件，很容易出现头文件路径、module map、架构切片或 Embed 设置不一致，直到链接或打包阶段才暴露。

我先确认消费端真正引用的是 `NaxclowMedia.xcodeproj` 里的 `NanoRTSP.xcframework`，然后核对产物结构：device 为 arm64 静态 archive，simulator 包含 arm64/x86_64。接着统一 `Headers`、`module.modulemap` 和公开 include 路径，把源码与测试中的 import 改到新的模块边界。

工程设置上保留 `Frameworks` 阶段的链接引用，但删除旧静态产物在 `Embed Libraries` 中的 `CodeSignOnCopy/RemoveHeadersOnCopy` 记录，因为静态库应该 link 而不是 embed。验证时不以“文件存在”为完成，而是检查 XCFramework plist、架构、模块编译，以及构建日志中的 `ProcessXCFramework` 和最终 `-lNanoRTSP` 链接。

这个案例的核心是先冻结 App/Media 对外合同，把变化限制在适配器和包边界，同时用真实消费端构建证明集成完成。

## 验证证据

- device `arm64` 静态库存在；simulator 包含 `arm64/x86_64`。
- XCFramework plist、公开 Headers 和 module map 完整。
- `NaxclowMedia.xcodeproj` 只保留链接引用，不再 embed NanoRTSP 静态库。
- 通用 iOS 构建记录出现 `ProcessXCFramework` 与实际链接；运行时弱网、媒体接收和长时间连接仍属于独立验证范围。

## 高频追问

### 静态 XCFramework 为什么不能 Embed？

静态库会在链接阶段进入最终 Mach-O，不是运行时加载的独立动态二进制。继续 Embed 会造成无意义复制、签名配置混乱，甚至打包错误。

### XCFramework 与普通 Framework 的区别是什么？

XCFramework 是多平台、多架构产物的容器，每个 slice 可以是 framework 或 library。关键不是后缀，而是每个 slice 的二进制类型、平台、架构和头文件/module map 是否匹配。

### 如何证明链接到的是新库？

同时检查 `ProcessXCFramework` 选择的 slice、最终链接命令中的库名、产物符号，以及旧 Framework 是否从 embed/link 路径移除。

## 代码与提交证据

- `8f1165a0`：静态 XCFramework、头文件、module map、import 和工程设置更新。

---

# P1：项目与职责通用问答

## 用 60 秒介绍 V720 项目和职责

V720 是智能摄像头 iOS App，核心链路包括实时预览、WebRTC 直播、设备控制、SD 卡回放、截图录像和文件下载。我主要负责 iOS App、Media 组件和设备 SDK 边界的问题定位与稳定性交付。工作重点不是单一页面开发，而是把页面生命周期、异步请求、设备回包、播放器状态和资源释放串起来，定位跨层问题，并通过最小修改和边界测试控制存量工程风险。比较有代表性的案例是回放首帧与失败回滚、设备协议短载荷崩溃，以及 NanoRTSP 静态 XCFramework 集成。

## 为什么命令发送成功，设备状态和 UI 仍可能不更新？

发送成功只代表本地调用或 DataChannel 写入成功，完整链路还包括设备执行、状态回包、消息路由、Model/Store 更新和 UI 刷新。任何一层的设备 ID、字段值、请求 attempt、页面生命周期或旧缓存不匹配，都可能导致不更新或被旧状态覆盖。回答时必须绑定一个真实请求，按“payload → route → readback → model → UI”逐层说明。

## HSBC Contact Details 为什么使用 VIPER？

VIPER 将 View、Presenter、Interactor、Entity 和 Router 的职责拆开，适合金融项目里规则复杂、多人协作和测试要求高的模块。收益是输入校验和业务行为可以脱离 UI 测试，路由边界也更明确；成本是模板代码较多，小需求可能跨多个文件。面试前需要补充一个真实需求的调用路径，以及你实际维护的测试和交付环节。

## 国际化和无障碍适配怎么讲？

不要只说 `NSLocalizedString` 和 `accessibilityLabel`。应讲文案长度、格式化和布局适配，以及 VoiceOver 顺序、Label/Hint/Traits、动态字体、点击区域和 UI 测试。只能说自己实际负责并验收过的部分。

---

# 暂不写入简历的表述

## Metal CPU 从 45% 降到 20%

在没有测试设备、码流分辨率、帧率、持续时间、工具、版本和多次采样记录前，只保留定性表述：减少逐帧 `UIImage` 创建、颜色转换、内存复制和主线程刷新。补齐证据后再恢复数字。

## Crash 率下降 40%

在不能说明监控平台、版本范围、统计周期、样本量、Crash-Free Users/Session 或特定模块口径时，删除百分比。可改为：修复多个直播、回放和协议边界的可复现崩溃与生命周期问题。

---

# 面试前本人确认清单

1. 三个案例中哪些分析、代码、测试和协调工作由本人完成。
2. 团队人数、负责周期、版本发布方式及是否能公开类名和协议细节。
3. HSBC 项目实际使用语言、本人负责的 VIPER 层次及测试数量。
4. 四家早期公司的英文法定名称和工作日期。
5. 所有数字是否有原始记录；没有证据就使用定性结果。
6. 每个案例录制一次 30 秒和 90 秒粤语回答，确保不用看稿也能讲清。
