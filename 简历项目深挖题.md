# 高级 iOS 简历项目深挖题

> 目标：把简历上的每个亮点转换成可以稳定讲 90 秒、并能继续承接追问的项目答案。
>
> 回答结构：**背景与约束 → 个人职责 → 根因或关键矛盾 → 方案与取舍 → 验证结果 → 复盘**。没有亲自参与或没有证据的数据不要补写。

---

# P0：V720 智能摄像头

## 01：用 60 秒介绍 V720 项目和你的核心职责

**回答骨架：**

- 产品形态、主要用户场景和技术栈。
- 自己负责的实时预览、WebRTC、设备控制、回放、录制和下载边界。
- 选择一个最能代表高级能力的结果：状态链路治理或 Metal 渲染优化。

**必须准备：** 团队规模、负责周期、代码 ownership、发布或线上问题处理方式。

---

## 02：WebRTC 直播从进入页面到首帧显示经历哪些阶段？

**回答骨架：**

- 页面生命周期与 session 创建时机。
- 设备注册、SDP 协商、媒体收包、H.264 解码和首帧渲染。
- DataChannel 建立、指令发送和资源释放与媒体链路的关系。
- 失败时如何按 Signaling、Media、Render、UI 分层定位。

**高风险回答：** 只背 WebRTC 标准流程，却说不清项目里的对象、日志点和失败边界。

---

## 03：为什么命令发送成功，设备状态和 UI 仍可能不更新？

**回答骨架：**

- 区分“发送成功、设备执行、状态回包、Store 更新、UI 刷新”五个阶段。
- 说明 DataChannel payload、设备元数据、状态路由和读回时序。
- 解释如何避免页面生命周期、旧状态缓存或多数据源造成覆盖。

**必须准备：** 一个真实问题的请求、回包、状态转换和最终修复点。

---

## 04：Metal 渲染为什么能把 CPU 占用从约 45% 降到约 20%？

**回答骨架：**

- 原链路中逐帧生成 UIImage、颜色转换、内存复制和主线程刷新的成本。
- 新链路如何把像素数据提交为纹理并交给 GPU 渲染。
- 说明线程模型、纹理复用、帧率控制和页面退出时的资源释放。

**必须准备：** 测试设备、码流分辨率、帧率、持续时间、工具及优化前后对比口径。

---

## 05：录制尺寸异常和下载状态串扰的根因分别是什么？

**回答骨架：**

- 录制：分辨率参数在哪一层丢失、覆盖或与实际帧尺寸不一致。
- 下载：任务状态为什么被页面、设备或多个文件共享。
- 修复时如何隔离单任务状态，并保证回调和 UI 更新在正确线程及生命周期内发生。

**必须准备：** 修复前复现条件、关键对象关系和回归范围。

---

## 06：替换 WebRTC 底层方案时，为什么要把兼容边界收敛在 Media 层？

**回答骨架：**

- 先冻结 App 层需要的播放、控制、回调和生命周期契约。
- 将 SDP、DataChannel、RTP、解码和渲染差异隔离在适配层。
- 说明如何灰度验证、保留旧实现或设计回滚边界。

**高风险回答：** 只说“减少改动”，但说不清冻结了哪些接口、如何验证兼容性。

---

## 07：线上 crash 率下降约 40% 是怎么统计和归因的？

**回答骨架：**

- 明确是全量 Crash、模块 Crash、Top Crash 还是特定链路的发生率。
- 说明版本范围、统计周期、样本量和监控平台。
- 列出自己解决的主要 crash 类型，以及如何排除同时期其他改动的影响。

**高风险回答：** 无法说明口径时不要使用“整体 crash 率下降 40%”。

---

# P1：汇丰银行个人金融项目

## 08：Contact Details 模块为什么使用 VIPER？它的成本是什么？

**回答骨架：**

- 说明 View、Interactor、Presenter、Entity、Router 的实际边界。
- 结合输入校验、页面跳转和测试隔离解释收益。
- 承认模板代码、跨层跳转和小需求改动成本，并说明团队如何约束。

**必须准备：** 一个真实需求从 View 到数据或路由层的调用路径。

---

## 09：手机号和邮箱校验规则如何做到动态更新？

**回答骨架：**

- 规则来自本地配置、接口还是业务模块；何时加载和刷新。
- 如何表达国家或地区差异、错误优先级和用户提示。
- 配置缺失、接口失败和旧规则缓存时如何降级。

**必须准备：** 规则模型、边界输入和至少一个失败场景测试。

---

## 10：金融 App 的国际化和无障碍适配有哪些难点？

**回答骨架：**

- 文案长度、复数和格式化、RTL 或语言切换对布局的影响。
- VoiceOver 顺序、Label/Hint/Traits、动态字体和可点击区域。
- 如何把多语言与无障碍要求纳入验收和自动化回归。

**高风险回答：** 只说用了 `NSLocalizedString` 和设置 `accessibilityLabel`。

---

## 11：XCTest、UITest 和 Jenkins 分别解决什么问题？

**回答骨架：**

- 单元测试覆盖规则和 Presenter/Interactor 行为，UITest 覆盖关键用户路径。
- Jenkins 负责触发、环境、报告、失败门禁和多语言组合。
- 说明如何处理 UI 测试不稳定、测试数据和异步等待。

**必须准备：** 实际覆盖的测试数量或关键场景、流水线触发方式及一次失败案例。

---

# P2：高级工程师能力

## 12：哪一次技术决策最能体现你的高级工程师职责？

**回答骨架：**

- 描述业务压力、技术约束和可选方案，而不是只讲最终实现。
- 说明你负责的分析、方案评审、拆分、协作和验证环节。
- 给出最终结果，并复盘如果重做会调整什么。

**建议优先案例：** Metal 渲染改造、WebRTC 兼容边界收敛、VIPER 模块交付。只能选择自己真正主导或深度参与的案例。

---

# 使用建议

1. P0 每题准备 30 秒和 90 秒两个版本。
2. 每题至少绑定一个真实类、日志、工具或数据口径。
3. 无法解释统计口径的数字先从简历移除或改成定性结果。
4. 模拟追问时重点练“为什么这样取舍”和“如何验证”，不要继续扩充术语。
